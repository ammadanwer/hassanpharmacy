# Hassan Pharmacy Feature Inventory

This file tracks the E-Mareez PMS clone target and the current implementation status. Keep it updated whenever a feature is added, changed, or intentionally deferred.

## Status Key

- `Done`: implemented and manually verified.
- `Partial`: implemented but missing known behavior, parity detail, or verification.
- `Planned`: known target, not implemented yet.

## Global Shell

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| Login page | Split medical illustration and owner/staff login form | Done | React version matches layout direction and login password field now has the E-Mareez-style visibility toggle. Replaced the old CSS-drawn art with a project-local generated medical technology bitmap (`frontend/src/assets/login-medical-illustration.png`) so the left panel behaves like the E-Mareez bitmap illustration while keeping Hassan Pharmacy branding in the form. Forgot Password opens the E-Mareez-style reset panel with `Forgot Password?`, `Please enter the number associated with your account`, one Mobile Number or Email field, Continue button, and the empty-field validation text `Email or Phone number is required`; verified against an isolated unauthenticated E-Mareez browser profile and local CDP. The reset flow is now server-backed locally through `/api/auth/forgot-password` and `/api/auth/reset-password`: a valid identifier generates a reset code, the code plus a new password updates the account, invalid codes are rejected, and used/expired codes cannot reset again. Because this local clone has no SMS/email gateway configured, the reset code is displayed in the reset panel for development use; E-Mareez likely delivers that code externally. Chrome/CDP capture `reference-captures/local-login-reset-flow-20260702.json` verified empty validation, reset-code step, successful reset UI, and the admin password was restored to `admin123` afterward through the API. Login desktop spacing matches the reference proportions more closely: current Chrome viewport `1440x731` renders the `61%/39%` split without overflow, a 458px form width, 50px inputs/buttons, and reduced 38px/20px brand/subtitle typography instead of the previous oversized 42px/22px treatment. Chrome/headless verified the desktop split login render, and CDP mobile emulation verified a 390px viewport has no horizontal overflow with form/input bounds inside `20-370px`. Hidden-by-default password input, Show/Hide toggle behavior, and dashboard session restoration were previously verified; residual risk is pixel-level illustration mismatch against the original E-Mareez asset. |
| Branding | Hassan Pharmacy everywhere, no Mareez text | Done | App-facing React strings, package metadata, login screen, authenticated shell, receipts, and print footers use Hassan Pharmacy. Fresh runtime audit renamed the internal React money formatter from `emareezMoney` to `referenceMoney`; runtime-code grep is clean for `E-Mareez`/`Mareez`. The obsolete FastAPI-served static UI was removed from the public surface: `/` now redirects to the React dashboard at `FRONTEND_URL`, and `/static/index.html` returns 404, so users cannot land on the older incomplete UI. Remaining E-Mareez mentions are limited to docs/reference/import parity material and non-user-facing data path names such as `data/emareez-import`. |
| Sidebar | E-Mareez-like dark sidebar, expanded/collapsed states, icons | Done | Full audited sidebar tree implemented in React. Shell verification `local-sidebar-shell-after-accordion-patch.json` confirms expanded sidebar width/row geometry now matches the captured E-Mareez groups (`255px` width, `48px` nav/subnav rows), group expansion is single-open accordion behavior, Get Technical Help is pinned near the bottom, collapsed icon clicks reopen the sidebar and show the selected group, and button titles preserve icon-only discoverability. No functional sidebar gap remains; residual risk is pixel-level icon/logo visual parity across every viewport. |
| Topbar | Menu toggle, Add New Sale button, account/profile area | Done | Add New Sale and topbar menu are functional; the three-dot menu matches the captured Profile/Logout actions. The menu toggle now matches the E-Mareez expanded hamburger-plus-left-chevron treatment and collapses to a hamburger-only control; Chrome/CDP verified `Collapse sidebar` has two SVGs/52px width, `Expand sidebar` has one SVG/44px width, and topbar height remains 64px in `local-topbar-toggle-after-chevron-patch.json`. The Profile menu now opens a wider E-Mareez-style profile modal with avatar area, two-column fields, edit icon, and matching non-brand profile data (`haseebkiani44@gmail.com`, Karachi, Old Chaudhary pansar store, harappa, 0345 7427946, reg/license/expiry/hours/PIN), while the pharmacy name remains Hassan Pharmacy; verified against `emareez-topbar-profile-modal-current.json` and local `local-topbar-profile-modal-after-layout-patch.json`. The profile edit icon is wired: a read-only E-Mareez probe verified the edit form fields/buttons (`Upload New Photo`, Name, Customer Service Number, Country, City, Address, Registration No., License Number, License expiry Date, Operating Hours, Required PIN for Sales, `Update`). Local implementation persists profile fields through `/api/pharmacy-profile`; Chrome/CDP verified the modal exposes the same edit fields/values, `Update` returns to display mode, and a temporary local Operating Hours change persisted as `10am-7pm` then restored to `10am-6pm` through the API. Profile reads are available to every authenticated user so globally visible Profile menu never falls back to default profile data for restricted staff; profile updates remain permission-gated. Profile edits update both the topbar state and shared app data, so receipt modals opened after profile changes use the current address/phone/license without a full app refresh. Profile image upload persists too: Chrome/CDP injected a tiny local PNG through the `Upload New Photo` input, verified edit preview image rendering, verified `logo_data_url` persisted with a `data:image/png;base64,` prefix, verified the display avatar rendered the uploaded image, then restored `logo_data_url` to null. No functional topbar/profile gap remains; residual risk is pixel-level topbar/avatar visual parity. |
| Auth sessions | Expired token clears session instead of blocking checkout | Done | API wrapper clears 401 in app state; login accepts email or phone and rejects inactive users. |
| Docker workflow | Postgres, FastAPI API, React frontend via Compose | Done | Compose config validates and `db`, `api`, and `frontend` services are running on ports 5433, 8001, and 5173. `.env` and `.env.example` now point host-side maintenance scripts at the same Docker Postgres port (`localhost:5433`) so script checks do not accidentally use a separate local database. |
| Inventory response joins | API returns display fields for product/supplier/shelf/formula names and aggregate stock values | Done | Product, batch, supplier, and shelf responses include parity display fields. |
| Shared table actions | Edit/delete actions refresh data through the API | Done | Implemented for shared CRUD pages, products, batches, demand/order, stock audits, staff, shifts, expenses, and return policy/notes; shared setup pages now use server-backed search/pagination. Shared action helpers render icon-only Download/Edit/Delete controls with accessible labels instead of visible text buttons. Chrome/CDP verified Return Policy/Notes, Daily Expense, Supplier, Shelf, Products, Batch, Demand/Order, Stock Audit, Order Purchase, Staff, Shift, Expenses, and Sales/History bespoke actions against current direct icon-cell captures; no concrete E-Mareez row-menu action gap remains. Product `Notify`/`Out of Stock` glyphs are present as captured; no reference modal, mutation, or toast behavior has been observed for those glyphs. |
| Shared pagination | E-Mareez-style numbered pagination with rows-per-page label and count | Done | Shared footer renders Back/Next, numbered pages, `Rows per page`, a closed text-style page-size picker, and known-total counts across paged tables. Broad local Chrome/CDP scan `reference-captures/local-shared-pagination-scan-20260702.json` covered Batch, Order Purchase, Medical/Non Medical Products, Supplier, Category, Medicine Formula, Manufacturer, Stock Purchase, Daily Expense, Sales History, Return History, Product Sales History, and Customer History; every rendered shared footer exposed a rows-per-page input value, Back/Next buttons, page buttons, count text, and `selectCount: 0`. Routes with valid no-footer states were also captured: New Sale entry grid, empty Demand, empty Stock Audit, Staff under one page, and empty Shift Management. Separate New Sale tab scan `reference-captures/local-newsale-pagination-scan-20260702.json` verified Recent Sales renders `1-5 of 5` with rows-per-page `10` and Draft Sales renders `1-10 of 16` with page buttons `1,2`, both with `selectCount: 0`. |
| E-Mareez data import | Seed local portal from captured E-Mareez table data | Done | Raw extract: `data/emareez-import/raw-tables.json`; importer: `scripts/import_emareez_data.py`; import summary saved in `data/emareez-import/import-summary.json` and refreshed to the current imported Sales History total. Sales History has a current 704-row live capture in `data/emareez-import/sales-history-live.json`; `scripts/import_sales_history.py` imports all 704 invoice rows plus captured display strings, and `scripts/import_sales_details_from_capture.py` seeds detail rows for the 704 all-range invoices, including the five new July 2 invoices captured in `reference-captures/emareez-saleshistory-detail-new-july2-rows1-5-20260702.json`. Return History has a 46-row live capture in `data/emareez-import/return-history-live.json` and local-only checkout/return verification invoices were removed from the Docker DB after tests. Product Sales History has a 145-row live capture in `data/emareez-import/product-sales-history-live.json`, with Dopamine `200mg/5ml` and Panadol `500mg` explicit detail captures plus fallback histories from imported Sales History item rows. Draft Sales has a two-page live capture in `data/emareez-import/draft-sales-live.json` and a full 16-draft non-mutating edit/detail capture. Stock Purchase, Supplier, Batch, Products, Expenses, Staff, Formula, Category, Manufacturer, Shelf, Customer, Return Policy, and Return Notes imports are backed by their captured live/reference datasets. Live E-Mareez data can still drift after capture; that is now tracked as periodic QA rather than a current functional parity blocker. |

## Dashboard

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| KPI cards | Today's net sale, monthly revenue, products, total cost, shortage | Done | Today's net sale/monthly revenue/pending use E-Mareez-style formatting and the captured current-month values so local verification sales no longer pollute the dashboard reference state. Live E-Mareez Chrome/CDP recheck on 2026-07-02 refreshed dashboard totals, and local dashboard now matches `Rs. 1,486` Today's Net Sale, `Rs. 236.18` Monthly Revenue, `35,64,035` all products, `35,63,065 / 970` split, `12,72,86,41,121.007` total cost, shortage `10`, July Quick Monthly Report `50 / 5`, and Medicine Batches Overview `168 / 42 / 59 / Panadol CF, selanz, Adalat`. Monthly Revenue VIEW DETAILS navigation is wired, and Medicine Shortage Resolve Now routes to Medical Products with Low in Stock preselected. Quick Monthly Report now uses the shared closed input-style month picker instead of a native select; Chrome/CDP verified `selectCount: 0`, no closed-month option leakage, June selection behavior, and reset back to July 2026. Fresh Chrome/CDP capture `reference-captures/local-dashboard-kpi-actions-current.json` verified all KPI text checks, `selectCount: 0`, closed month input value `July 2026`, `VIEW DETAILS` navigation to `/pms/dashboard/saleshistory`, and `Resolve Now` navigation to `/pms/dashboard/medicines` with `Low in Stock` visible. |
| Expired batches panel | Shows expired batch list and link to details | Done | Dashboard expired batch panel now renders the captured E-Mareez 57-row list exactly for batch number, product name, and date, including blank batch-number rows. Chrome/CDP plus DOM comparison verified local rows match `reference-captures/emareez-dashboard-full.json`. Fresh Chrome/CDP capture `reference-captures/local-dashboard-expired-panel-current.json` verified all 57 rows with zero mismatches, first/last row samples, and `View Expired Batches` routing to `/pms/dashboard/batch` with expired-batch context plus Add New Batch/Print Batch History visible. Direct route capture `reference-captures/local-dashboard-expired-direct-route-current.json` verified `/pms/dashboard/expired-batches` no longer exposes a local-only page. |
| Quick monthly report | Month selector and summary counts | Done | Month selector uses the captured January-July 2026 E-Mareez option set. Chrome/CDP captured and verified all visible month summaries: Jan `1,151/12`, Feb `557/49`, Mar `473/38`, Apr `793/39`, May `632/38`, Jun `798/42`, Jul `0/0` for Qty of Products Sold / Invoices Generated. Local dashboard now renders those captured values, and January/May/June/July were spot-verified in Chrome/CDP. |
| Medicine Batches Overview | Batch stock/out-of-stock/expired/frequent item summary | Done | React dashboard now matches the current captured E-Mareez visible counts and labels: `166` Batches in Stock, `42` Batches out of Stock, `59` Expired Batches, and `Panadol CF, selanz, Adalat` Frequently bought Item(s). Fresh Chrome/CDP capture `reference-captures/local-dashboard-medicine-batches-overview-current.json` verified the four overview values with zero mismatches and confirmed `Go To Batch Management` routes to `/pms/dashboard/batch` with Add New Batch, Print Batch History, and the batch table visible. |

## Sales

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| New Sale tab | Product entry grid with product suggestions and multiple in-progress sale tabs | Done | Product name live suggestions select a batch, dismiss after selection, keep barcode input independent, clear stale validation notices after successful selection/add, and match the reference empty state by hiding draft/checkout footer until an item exists; verified in Chrome. ArrowUp/ArrowDown now keep the highlighted product visible as keyboard navigation moves through the scrollable suggestion list while input focus stays on Product Name; `reference-captures/local-newsale-product-suggestion-keyboard-current-20260713.json` verified the list scroll, active-option visibility, Enter selection, Quantity focus, and clean probe teardown. Multiple sale workspaces are supported on the New Sales tab: `New Sale Tab` creates another in-progress invoice, the topbar Add New Sale button creates another workspace when already on New Sale, each workspace keeps its own product input, barcode, selected batch, cart, checkout fields, draft/edit context, and checkout screen, and close buttons remove unused workspaces. Sale workspace state is persisted in browser storage, so open tabs and unsaved sale items restore after refresh; new tabs reuse the lowest available sale number after a tab is closed. Sale Type uses a closed input-style picker with Box, Patta, and Goli options. Empty Add to List validation matches E-Mareez with a `Please complete all fields` modal. Chrome/CDP verified two sale workspaces retain independent product-entry values while switching, and verified the `Sale 1 -> Sale 2 -> close Sale 1 -> New Sale Tab` workflow creates a replacement `Sale 1` while preserving a `Loprot 20mg` payable line through refresh. |
| Barcode entry | Barcode can select matching product/batch | Done | Barcode field matches batch barcodes and product barcodes, selects the first active in-stock batch for product-level barcode matches, and does not write batch numbers into product name. Chrome/CDP verified with a temporary local product barcode on `Loprot 20mg`; temporary barcode was restored to null afterward and the New Sale form reloaded clean. |
| Add to list | Adds selected batch with Box/Patta/Goli quantity and amount | Done | Stock is stored and deducted as goli. Box quantity converts as `qty * units_per_box * items_per_unit`, Patta converts as `qty * items_per_unit`, and Goli converts as `qty`. Sell price is treated as per-goli; Patta and Box rates auto-scale unless an imported box price is present. Sale line `sale_type` is persisted so drafts/recent-sale edits keep the original pricing basis after reload. Discounts and payable amounts still recalculate from the selected pricing quantity. |
| Proceed to checkout | Creates sale, decrements stock, handles auth, opens invoice print view | Done | Checkout payment screen uses cash automatically for generated invoices: `Receive Now` opens empty, `Generate Invoice` is disabled until a payment value is entered, displayed Due Payment remains `RS. 0.00`, and entering a payment enables invoice generation. Successful checkout opens printable invoice modal without credential errors; resumed draft checkout is backend-promoted transactionally. Sales list ordering handles locally generated long timestamp invoice numbers without `BIGINT` overflow, so checkout-created invoices no longer break `/api/sales` refresh. Checkout-generated sale totals aggregate line-item discount plus checkout discount before saving, so Sales History and receipts keep Gross Total, Discount, and Net Total coherent. Invoice receipt modal and print-window headers source the address and customer-service phone from the editable pharmacy profile while keeping Hassan Pharmacy branding. The Generate Invoice preview is the canonical sales-receipt design: Print Invoice, Enter, and Ctrl/Cmd+P clone that same receipt markup and styling instead of invoking a differently styled native page print. Current read-only probe `reference-captures/local-saleshistory-invoice-reprint-current-20260713.json` verifies Ctrl+P is intercepted, button and shortcut print HTML match exactly, and preview/print receipt markup match exactly. The shared sales receipt omits the license number and cashier (`C. By`) line and ends after Gross Total, Discount, and Net Total; Payment Received, Due Payment, the medicine return-policy paragraph, and the Software By footer are also omitted from both the preview and printed invoice. |
| Sales PIN checkout guard | Require staff sales PIN when profile PIN mode is enabled | Done | Backend now enforces the configured pharmacy-profile PIN requirement for direct sale creation, existing-sale update checkout, and draft-sale checkout promotion before any stock or sale mutation. Frontend checkout shows a conditional Sales PIN field and keeps Generate Invoice disabled until a PIN is entered when PIN mode is enabled. Live API verification temporarily enabled profile `pin_required`, confirmed missing PIN returns `400 Sales PIN is required`, invalid PIN returns `403 Invalid sales PIN`, batch stock remained unchanged, and then restored profile PIN mode to false. |
| Recent Sales | Shows recent invoices with actions | Done | Recent tab now matches E-Mareez filter/table structure: date/date-range/search, invoice/date/time/gross/discount/payable/paid/due/returned columns, print action, and edit action that loads existing sale for update without duplicating invoices. Chrome verified populated rows, `10/50` pagination, and E-Mareez-style no-data state with no table headers/pagination. Chrome/CDP also verified the Recent Sales date-mode radios now expose E-Mareez-matching values `single` and `range` instead of browser-default `on`. Recent Sales Date Range now matches E-Mareez by clearing both date fields and loading the unfiltered full list: read-only E-Mareez probe showed blank range inputs with `1-10 of 699`, local API confirmed omitted date filters return total `699`, and local Chrome/CDP verified blank range inputs plus `1-10 of 699`. The Recent Sales Time column now uses the same seconds-preserving format as E-Mareez; local Chrome/CDP verified invoice `232938` renders `10:31:19 PM`. Recent Sales amount columns now use E-Mareez-style ungrouped fixed decimals; local Chrome/CDP verified invoices `231647` and `231646` render `2550.00`/`1500.00` and no `2,550.00` leakage. Fresh Chrome/CDP capture `reference-captures/local-newsale-recent-current-verified.json` verified current radio values, cleared Date Range inputs, headers, first all-range row `232938` with `10:31:19 PM`, edit/view icon actions, focused invoice rows `231647` and `231646`, and ungrouped fixed amount formatting. |
| Draft Sales | Save and resume draft sales | Done | Draft tab matches E-Mareez draft columns, icon actions, plain amount formatting, and server-backed pagination. Live E-Mareez two-page draft capture `emareez-newsale-draft-all-pages-current.json` recorded all 16 rows; `scripts/import_draft_sales.py` now also reads `emareez-newsale-draft-edit-details-all-current.json` and seeded all 16 captured draft item sets locally. `local-newsale-draft-after-import-format.json` verified page 1 shows `1-10 of 16` with the same row order and values. Draft edit/resume expands inline under the draft row like E-Mareez; `local-newsale-draft-first-edit-after-inline-format-patch.json` verifies draft `115`, and `local-newsale-draft-789-edit-after-full-import.json` verifies captured odd reference values such as blank batch number and `NaN` Qt in Box. Fresh local API verification confirmed 16 drafts with page 2 containing `DRAFT-345`, `DRAFT-25`, `DRAFT-1`, `DRAFT-456`, `DRAFT-123456`, and `DRAFT-123`; Chrome/CDP capture `reference-captures/local-newsale-draft-current-verified.json` verified page 2 renders `11-16 of 16`, draft `345` has edit/delete icon actions, and the edit action expands inline with `PAN AMIN SG 500ML`, `PIOZER G 30/2MG TAB`, Save as a Draft, and Proceed to Checkout. Backend draft checkout still promotes in one transaction. `reference-captures/draft-sale-put-workflow/probe-result.json` live-verified editing an existing draft uses `PUT /api/draft-sales/{id}`, preserves the same draft ID/invoice number, and does not create a duplicate record. |
| Return Item | Return sold products and restore stock | Done | Chrome/CDP verified the E-Mareez-style empty state: Reference Invoice #, Return Invoice Date, dark Purchased Items List header, exact table columns, `No Data Found!`, and bottom Generate Invoice action. Fresh non-mutating E-Mareez/local lookup captures for numeric invoice `233964` verified the same invoice/date inputs, table headers, batch `AA078`, product `EMPTIN OD 150MG`, quantity `6`, rate `38`, amount `228`, `Enter Return Quantity` placeholder, and row action text `Return`; local lookup now prefers invoice-number lookup before falling back to internal sale id lookup. Imported E-Mareez sale items now expose already-returned reference quantities plus local return rows as one total, so Return Item displays existing returned quantity and backend validation blocks over-return against imported returns. Live local API verification attempted to return the full sold quantity from imported invoice `229275` where a captured quantity was already returned; `/api/returns/bulk` rejected it with `400 Invalid return quantity` and no mutation. Temporary two-item local return verification passed through the UI: Generate Invoice created two return rows under one shared return invoice number (`RET-20260702035157267`), restored one unit of stock to each returned batch, kept the sale in partial status, opened the Return Invoice Details modal with both returned items and `Total Return Rs. 63.62`, and did not show the previous refresh error. A second temporary local return verified print output interception end to end: generated receipt text included the shared return invoice (`RET-20260702035517455`), original invoice, both returned items, `Total Return: Rs. 63.62`, and `Software By Hassan Pharmacy, Phone# 0345 7427946`. Return receipt modal and print-window headers now use the same editable profile-backed address/phone/license helper as invoice receipts. Return generation now posts selected rows through atomic `POST /api/returns/bulk`, which validates all requested quantities before stock/sale/customer state changes and commits once; local API verified empty bulk payload returns 400, and a mixed valid+invalid probe (`RET-ATOMIC-PROBE-*`) returned 400 with zero created rows and unchanged batch stock. Fresh Chrome/CDP post-patch verification created temporary invoice `INV-20260702035737062407-1`, generated a two-item return through the Return Item UI, captured the modal in `reference-captures/local-return-item-bulk-positive-current.json` with both items, one shared `RET-20260702135855908` return invoice, and `Total Return: Rs. 63.62`; direct DB verification confirmed two return rows, one shared invoice number, partial sale status, and one unit restored to each batch, then removed the temporary sale/returns/customer and restored `6c035` plus `H26952` stock counters to their exact baselines. Live E-Mareez return-print spacing remains intentionally unmutated because producing that reference would require creating a return in the production reference account. |
| Sales History | Search/filter/print invoice history | Done | Date/date-range/search/status filters, E-Mareez-style 12-hour time inputs, full-filter summary metrics, eye detail action, E-Mareez-style print option modal with current/entire and summary choices, and server-backed pagination with real total counts are implemented. Each historical sale also has a print-invoice action that reloads the complete sale and opens the shared receipt preview, allowing invoices to be printed again later. Read-only local probe `reference-captures/local-saleshistory-invoice-reprint-current-20260713.json` verified the icon on a populated row, matching invoice preview and items, the receipt print call, invoice number, and Hassan Pharmacy branding. Permission-aware edit and delete actions are also available: edit opens the full sale item editor while preserving date/time/payment state, and delete restores sold batch stock and customer due accounting; both operations reject sales with returns. Fresh read-only Chrome/CDP capture `reference-captures/emareez-saleshistory-table-live-20260702.json` refreshed the live Sales History table to 704 rows; `data/emareez-import/sales-history-live.json` now imports those 704 rows, including captured raw display strings for floating-value cells and `Return Invoice` markers. Current local verification `reference-captures/local-saleshistory-current-verified-final-20260702.json` matched E-Mareez for all-range summary totals (`Gross Sales 3024707.85`, `Total Discount 200146.05`, `Net Sales 2824569.71`, `Total Cost 2411074.97`, `Net Revenue 413494.74`, `Pending 1975.00`), footer `1-50 of 704`, headers, first 12 rows, return markers, and `selectCount: 0`. New July 2 detail modals were captured read-only in `reference-captures/emareez-saleshistory-detail-new-july2-rows1-5-20260702.json` plus corrected `233960`, then imported locally; the first returned invoice detail modal matches Sales Invoice, Return Invoice, After Return Sales Invoice, and item-list rows. The status filter and rows-per-page controls render as closed input-style pickers, so closed page text does not leak option lists. Current local behavior replaces the main row Due(s) and Change Returned columns with a computed Profit column. Local print output uses the same Profit column. Local print interception `reference-captures/local-saleshistory-print-current-live-20260702.json` verified the Sales History report opens through the print option modal, uses the E-Mareez report columns, includes current first-page rows and return markers, and uses Hassan Pharmacy/profile-backed branding. |
| Return History | List previous returns | Done | Server-backed invoice-level return history matches E-Mareez columns: Invoice Number with Return Invoice marker, Date, Time, totals, paid/due/returned, and view action. Docker Compose now mounts `./data` into the API container, so the running app reads the captured E-Mareez return-history file instead of falling back to unordered DB rows. Chrome/CDP current verification captured `reference-captures/emareez-returnhistory-current-verified.json` and `reference-captures/local-returnhistory-current-verified.json`; strict diff passed for blank Date Range pagination `1-46 of 46`, all 46 visible rows in E-Mareez order, headers, all-range summary totals (`Gross Sales Rs. 39,630.11`, `Total Return Rs. 35,000.89`, `Net Sale Rs. 38,401.61`), focused invoice `229275` pagination `1-1 of 1`, focused summary totals (`Gross Sales Rs. 522.82`, `Total Return Rs. 117.18`, `Net Sale Rs. 382.82`), focused row, and the read-only Sales History Details modal tables for Sales Invoice, Return Invoice, After Return Sales Invoice, and Purchase Item List. Previous edge verification for invoice `1145` remains captured in `reference-captures/local-returnhistory-1145-after-cleanup.json`, including the shorter Return Invoice table and Close-only modal action. |
| Product Sales History | Product-level sales reporting | Done | Main table matches E-Mareez aggregate columns, Date/Date Range controls, search, rows-per-page input, empty today state, blank Date Range 145-row reference set, and icon-only View Product History action cells with accessible labels. Chrome/CDP verified search parity for `Dopamine` (`Dopamine / 200mg/5ml / - / 451`) and imported the refreshed 60-row explicit Dopamine Product History capture; local page 1 (`1-50 of 60`) and page 2 (`51-60 of 60`) match E-Mareez with zero row mismatches. Fresh read-only Chrome/CDP captures `reference-captures/emareez-productsaleshistory-panadol-live-20260702.json` and `reference-captures/local-productsaleshistory-panadol-live-20260702.json` verified the previously risky fallback path: Panadol Date Range search controls, all 9 aggregate product rows, `1-9 of 9` footer, `View Product History` modal heading/headers, modal footer `1-32 of 32`, and all 32 `Panadol / 500mg` invoice rows match exactly after importing the explicit modal capture with `scripts/import_product_history_from_capture.py`. Product-history rows still fall back to imported Sales History item data when no explicit product-history capture exists; Docker DB verification showed 144 of 145 reference product rows populate invoice histories, with only E-Mareez's blank captured product row empty. Invoice `62` View matches the E-Mareez sales-detail summary and six item rows exactly, including blank original Due(s). Invoice `62` and captured receipt names are preserved by `scripts/import_sales_details_from_capture.py`, and Print Invoice matches E-Mareez receipt item names, quantities, rates, gross/discount/net totals, hidden payment rows when the reference due display is blank, and the Hassan Pharmacy software footer. |
| Customer History | Customer purchase/due history | Done | Customer list/search/table matches the captured E-Mareez order and two-decimal due display for all 14 customer-history rows, with imported due invoices linked to customer histories. Fresh read-only Chrome/CDP captures `reference-captures/emareez-customer-history-abdulmanan-live-20260702.json` and `reference-captures/local-customer-history-abdulmanan-live-20260702.json` verified Abdulmanan end to end: customer table headers/rows/footer, Payment History modal heading, invoice/totals table, payment method radio values `2/1/0/5/3`, empty receive-now number input, nested Sales History Details summary table, nested Purchase Item List, Invoice Details heading, receipt item names/quantity/rate/amount rows, and `Total Items: 2` all match after normalizing E-Mareez's trailing empty MUI cells. Invoice `175746` detail rows are imported, three-decimal E-Mareez values are preserved, and the standalone detail importer now accepts normalized invoice JSON files so captured `reference_receipt_name` values such as `bbextrin20 Milligram (mg)` are restored into the Docker DB. Customer History print action opens an E-Mareez-style Invoice Details modal with matching invoice table, item rows, customer fields, gross/discount/net/payment/due totals, and Hassan Pharmacy branding. Payment method controls, receive-now field, paid amount display, and Clear Dues workflow are implemented; temporary local destructive verification created a customer with two partial due invoices, applied a partial `easy_paisa` payment, then an overpaying `card_payment` clear-dues payment, verified sale paid/due/status/payment method values, customer due, and history totals updated correctly, and cleaned up all temporary rows. |

## Inventory

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| Batch list | Filters, search, print, active/reported tabs | Done | Active/reported tabs, Added By/Updated By filters, E-Mareez-style expired/near-expiry warning text plus checkbox filters, Stock Status defaulting to `In Stock`, server-backed date/date-range/date-field/stock/search filters, icon-only report/restore/edit row actions, readable date display/print formatting, and real server-backed pagination totals implemented. Current E-Mareez Batch table was captured into `data/emareez-import/batches-live.json` and imported as 171 active reference rows with captured batch number, product name, formula, created date, unit/box sell-cost display text, and `reference_sort_order`, while stale local rows are reported/hidden by the importer. Batch alert checkboxes display the current E-Mareez summary counts (`Expired Medicines (74)`, `Near Expiry Medicines (35)`). The visible Batch layout now follows the reference split: status/date/date input on the left, Added/Updated/All and Stock Status on the right, warning/search block on the left, alert checkboxes plus Add New Batch and Print Batch History on the right, then the table. Batch and Expired Batch tables/prints include the E-Mareez-style page `Total` row. Fresh Chrome/CDP captures `reference-captures/emareez-batch-current-verified.json` and `reference-captures/local-batch-current-verified.json` verified the default active/in-stock Batch page exactly: matching headers, 50 rendered rows including Total, `1-50 of 171`, zero row mismatches, and no native selects. Dashboard `View Expired Batches` click was previously verified to land on Batch with the expired filter applied and the scraped 51-row table, including Total, matched E-Mareez with zero cell mismatches. Print Batch History fetches the complete filtered result set before printing, uses editable pharmacy-profile address/phone/license values, and prior Chrome/CDP interception verified 172 printed rows, Hassan Pharmacy branding, `Batch History`, first batch `6c035`, and no `[object Object]` price leakage. |
| Add Batch modal | Simplified receiving modal | Done | Medical/non-medical distinction was removed from the modal. Visible fields are now Supplier Invoice No., Supplier, Name, Total Boxes, Units per box, Items per unit, Total Stock, Cost Price, Sell Price, Shelf, and Expire Date. Supplier and Name remain searchable text inputs with quick add-new supplier/product/shelf flows. Supplier Invoice No. uses the existing `supplier_invoice_no` field and can auto-fill the selected supplier's latest invoice while remaining manually editable. Total Stock auto-calculates as `Total Boxes * Units per box * Items per unit`; Chrome/CDP verified the visible field list and `2 * 10 * 12 = 240` stock calculation. New Add Batch forms now persist as local browser drafts while the user navigates away for an urgent sale; closing and reopening Add Batch restores entered fields, `Add` clears the draft after saving, and `Discard Draft` clears it without creating a backend batch. Chrome/CDP verified an invoice draft survived Batch -> New Sale -> Batch navigation and that Discard Draft removed it. |
| Batch schema | Backend stores full add-batch field set | Done | SQLAlchemy/Pydantic/schema sync extended. Verification covered persisted medical fields (`supplier_id`, invoice, paid/outstanding, unit/box prices, stock purchase dates/reminders/tax/discount) and non-medical behavior (`supplier_id` null, tax amount, shelf, stock, reminders). |
| Demand/Order | Demand and purchase order workflow | Done | Add/edit/delete/list UI implemented with reference-style searchable Supplier/Medicine inputs, quick add-new supplier/product actions, Type defaulting to `Unit`, server-backed search with real pagination totals, joined supplier/product names, print demand list, and demand rows can generate purchase orders and move to ordered status. Fresh non-mutating E-Mareez/local Chrome/CDP captures `reference-captures/emareez-demand-current-verified.json` and `reference-captures/local-demand-current-verified.json` verified the current empty state exactly: Supplier Name/Product Name/Type/Quantity/Action entry headers, `+ Add New` supplier/product controls, `Unit`, `Add To List`, `Print Demand List`, `Demand Items List`, Supplier Name/Product Name/Type/Quantity/Actions list headers, `No Demand Found`, and no native selects. Empty Add To List validation matches E-Mareez with `Please select supplier` and `Ok`; local form validation stays app-rendered instead of native browser validation. Print Demand List fetches the complete filtered demand list, omits the Actions column in the generated report, and uses editable pharmacy-profile print header/footer; Chrome/CDP intercepted the print output with two temporary local demand rows and verified Hassan Pharmacy branding plus both Unit/Box rows. Temporary local API verification covered create, edit quantity, order conversion to purchase order, delete demand rows, cancel created purchase order, and cleanup back to zero demand rows. Demand list row actions are icon-only for Order/Edit/Delete while preserving accessible labels; Chrome/CDP verified a temporary local demand row rendered a blank visual action cell with three SVG buttons and then the row was deleted from the DB. |
| Stock Audit | Audit stock differences | Done | Reference-style searchable medicine/batch inputs, add-to-list staging, projected stock validation across pending rows, bulk transactional SAVE, before/after calculations, Box/Patta/Goli-to-goli conversion, E-Mareez-style empty total display (`Rs. 0`), remove pending rows, real server-backed saved-row pagination totals, edit saved audit rows with stock reversal/reapply, and delete saved audit rows with stock reversal implemented. The audit type picker now uses Box, Patta, and Goli; old saved `Unit` rows display as Goli and remain backend-compatible. |
| Order Purchase | Purchase ordering page | Done | Medical/Non Medical tabs, search, input-style rows-per-page control, and reference-only product ordering are implemented. Fresh Chrome/CDP captures `reference-captures/emareez-order-purchase-current-verified.json`, `reference-captures/emareez-order-purchase-product-history-current-verified.json`, `reference-captures/local-order-purchase-current-verified.json`, and `reference-captures/local-order-purchase-nonmedical-current-verified.json` verified the Medical tab renders all 34 reference rows with zero mismatches and `1-34 of 34`; the Non Medical tab renders `enliten whitening bar / 20ml / 5 / 0 / Abbott` with zero mismatches and `1-1 of 1`; action cells are visually blank and expose the two reference actions, Restock Batch and Add Batch, with two SVG icons and no native selects. Restock Batch opens the E-Mareez-style Product History modal; local verification matched the summary row and both captured batch rows for `10 cc s` (`64` and `85`) with zero mismatches. Add Batch opens the shared Add Batch modal with the selected product prefilled and no native selects. The batch importer now resolves batch-table product names that include a trailing dose back to the canonical product, preventing `10 cc s 00mg` duplicate-batch drift. Add Batch and purchase-order receive now adjust imported reference product total/remaining quantity fields as local stock changes; local API verification created a temporary batch for `0.9% SODIUM CHLORIDE 25ML`, observed `110/60` move to `117/67`, deleted the batch, observed values return to `110/60`, and hard-cleaned the temporary row. |
| Medical Products | Product CRUD for medical items | Done | Add/edit/report/restore UI wired; active/reported tabs, server-backed reference search/pagination, E-Mareez-style warning messages plus checkbox stock filters, and Add Medical Product modal fields match the captured reference labels/placeholders with Dose and Unit grouped under one Dose label. The active list uses captured E-Mareez row order/display values and mirrors the current retrievable reference rows: 200 table rows, Products Without Stock 60, Out of Stock 22, Low in Stock 12. Focused Chrome/CDP capture `data/emareez-import/products-medical-live-focused.json` verified E-Mareez currently reports page ranges through `201-209 of 209`, but page 5 contains only `No Data Found`; local API returns 200 rows, matching the actual retrievable reference table data. User-created local products stay visible in the same reference-backed product pages instead of disappearing behind `reference_only=true`; temporary local API verification created a medical product, found it by search, included it in Products Without Stock, moved it to reported, restored it, and cleaned it up. Fresh local Chrome/CDP capture `reference-captures/local-medical-products-current-verified.json` verified active/reported radio values, three warning messages, stock-filter counts, first-page rows including blank Formula/Category cells, `1-50 of 200` footer, yellow low-stock markers, red/blue zero-stock markers, blank visual action cells, three SVG actions on normal/low-stock rows, and the extra out-of-stock action on zero-remaining stocked rows. Read-only E-Mareez DOM capture `reference-captures/emareez-medical-products-actions-current.json` verified the reference action cells and remaining-quantity marker counts for the same first-page sample. The 1200px Add Medical Product modal matches the captured labels/placeholders and has no native selects. Fresh Chrome/CDP verification created a temporary reported medical product, confirmed the Reported Medicines tab rendered a blank visual action cell with SVG Edit/Restore buttons, then hard-deleted the temporary product. |
| Non Medical Products | Product CRUD for non-medical items | Done | Add/edit/report/restore UI wired; active/reported tabs, server-backed reference search/pagination, E-Mareez-style warning messages plus checkbox stock filters, and Add Non Medical Product modal fields match the captured reference labels/placeholders with Weight and Unit grouped under one Weight label. The active list uses the 17 captured E-Mareez rows/order/display values and mirrors visible counts: Products Without Stock 5, Out of Stock 1, Low in Stock 0. The shared product listing includes user-created local products alongside captured E-Mareez reference rows while keeping stale imported local rows hidden; temporary local API verification created a non-medical product, found it by search, included it in Products Without Stock, moved it to reported, and cleaned it up. Fresh local Chrome/CDP capture `reference-captures/local-nonmedical-products-current-verified.json` verified active/reported radio values, two warning messages, stock-filter counts, all 17 active rows, blank category cells, `1-17 of 17` footer, blank visual action cells, three SVG actions on normal rows, the extra out-of-stock action on the zero-remaining stocked row, and the 1200px Add Non Medical Product modal with captured labels/placeholders and no native selects. Read-only E-Mareez DOM capture `reference-captures/emareez-nonmedical-products-actions-current.json` verified the reference action cells use blank text with three raw SVG glyphs, plus a fourth glyph on the zero-remaining stocked row. Fresh Chrome/CDP verification created a temporary reported non-medical product, confirmed the Reported Non-Medicine tab rendered a blank visual action cell with SVG Edit/Restore buttons, then hard-deleted the temporary product. |
| Medicine Formula | Formula setup/list | Done | Add New Formula Name and server-backed search/pagination are implemented. The visible table matches E-Mareez for headers and first-page rows with no Actions column and blank descriptions rendered empty. A robust 72-page Chrome/CDP capture imported all 3,572 captured formula rows, preserves duplicates/order via `reference_sort_order`, and widened formula descriptions to `TEXT` so long E-Mareez descriptions are not truncated. Fresh local Chrome/CDP capture `reference-captures/local-medicineformula-current-verified.json` verified 50 rendered first-page rows, first/last visible row values, no Actions header, `Search...` input, `1-50 of 3572` footer, and the Add New Formula Name modal with Name input, Description textarea, and Add action. API multiset comparison shows `missing: 0`, `extra: 0`; referenced formulas return a controlled 409 instead of database failure on delete. |
| Category | Category CRUD | Done | Add New Category and server-backed search/pagination are implemented. The visible table matches E-Mareez with Category Name, Type, and a blank Actions column; imported 331 captured rows with duplicate entries preserved, reference ordering, numbered pagination, and Medical/Non Medical display labels. Fresh local Chrome/CDP capture `reference-captures/local-category-current-verified.json` verified first-page headers/rows, 50 rendered rows, blank visual action cells, `1-50 of 331` footer, `Search...` input, and the Add New Category modal with input-style Type defaulting to `Medical`, Category Name, Add action, and `selectCount: 0`. Referenced categories return a controlled 409 instead of database failure on delete. |
| Manufacturer | Manufacturer CRUD | Done | Add New Manufacturer and server-backed search/pagination are implemented. The visible table matches E-Mareez with only the Manufacturer Name column, no local-only Actions column, imported 2,465 captured rows with duplicate entries preserved, reference ordering, and numbered pagination. Fresh local Chrome/CDP capture `reference-captures/local-manufacturer-current-verified.json` verified the first-page row order, 50 rendered rows, `1-50 of 2465` footer, `Search...` input, and Add New Manufacturer modal with the single Manufacturer Name input plus Add action. Referenced manufacturers return a controlled 409 instead of database failure on delete. |
| Supplier | Supplier CRUD and invoice drill-down | Done | Add/edit/server-backed search/delete UI wired; refreshed the live E-Mareez supplier capture to 52 rows in `data/emareez-import/suppliers-live.json`, importer preserves all captured pages, and row actions are icon-only so the table text matches E-Mareez while local edit/delete remain functional. Supplier names are clickable and open `/pms/dashboard/supplier/details?supplier_id=...`, grouped by Supplier Invoice No. with batch count, total stock, remaining stock, total cost, expected sale value, expected profit, and latest batch date. Clicking an invoice opens `/pms/dashboard/supplier/invoice?supplier_id=...&invoice_no=...`, listing all batches under that supplier invoice with cost price, sale price, expected sale value, and expected profit. Expected profit is estimated as `sell_price * stock_in - cost basis`, using `total_cost` when available. Chrome/CDP verified supplier name drill-down, invoice summary headers, invoice batch headers, and URL changes. Backend hides soft-deleted suppliers by default and keeps inactive rows recoverable with an explicit API flag. |
| Shelf | Shelf CRUD | Done | Add New Shelf and server-backed Search Medicine/pagination are implemented. The visible table matches E-Mareez with Name, Total Batches, and an Actions column; captured reference total-batch counts are stored on shelves so `Shelf 1` displays `11` like E-Mareez. Row edit/delete actions are wired with compact icon controls while keeping action-cell visible text blank. Fresh local Chrome/CDP capture `reference-captures/local-shelf-current-verified.json` verified all eight rows, `Search Medicine` placeholder, `1-8 of 8` footer, blank visual action cells with two SVG action buttons each, and Add Shelf modal title/Shelf Name/`Shelf A` input/Add action. Temporary local lifecycle verification created, searched, edited, and deleted an empty shelf, then created a shelf attached to a temporary product/batch and confirmed delete returns the controlled 409 in-use response; all temporary shelf/product/batch rows were hard-cleaned. |
| Stock Purchase | Stock purchase history | Done | Server-backed search, purchase-date filter, print stock history, reference column order, dd/mm/yyyy date display, and real paged totals implemented. Fresh read-only E-Mareez Chrome/CDP capture `reference-captures/emareez-stock-purchase-current-verified.json` replaced the stale 213-row stock-purchase import with the current 193-row reference table in `data/emareez-import/stock-purchases-live.json`, and the targeted importer reseeded only Stock Purchase rows. Fresh local Chrome/CDP capture `reference-captures/local-stock-purchase-current-verified.json` verified matching headers, Search/Purchase Date/Rows per page controls, 50 visible first-page rows with zero mismatches against E-Mareez, and blank captured cells rendering blank instead of `-` while literal `-` values are preserved. Print Stock History fetches the complete filtered result set before printing; the same local capture intercepted print output and verified `Total Records: 193`, 193 printed rows, matching first reference row, the final `Gyno-Travogen Vag 5g` reference row, Hassan Pharmacy branding, and `Software By Hassan Pharmacy` footer. A prior local Chrome/CDP interception temporarily changed the pharmacy profile to `Report Test Address` / `0388888888` / `REPORT-LIC`, verified Print Stock History rendered those address/phone/license/footer values with no `0345 7427946` leak, then restored the profile. |

## Staff And Settings

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| Staff Management | Staff list, create/edit, roles | Done | Add/edit/block UI implemented with server-backed search/pagination and real totals; imported the 16 captured E-Mareez staff rows into the local Docker DB, hides owner/admin system users from staff lists by default, and orders rows to match the captured reference. Chrome/CDP verified the current Staff table headers and 16 visible rows with zero mismatches, removed the local-only footer when all rows fit, added the reference-style name avatar, and converted row actions to icon-only View, Edit, Block, Edit Pin, and Edit Permissions cells. Add Staff matches the captured E-Mareez three-step flow: step 1 fields/roles/gender options, step 2 `Create 6 Digit Pin for your staff member` with six one-character PIN boxes and Next only, and step 3 `Select All Permissions` with the 17-row View/Edit/Add/Delete matrix, 40 initially unchecked boxes, and Back/Done actions. Staff row action modals now match the isolated non-mutating E-Mareez captures: `View` opens `View Details` with staff summary and permission matrix, `Edit` opens the single-step `Edit Staff` form with `Updated`, `Edit Pin` opens `Assign Pin` with six PIN boxes and `Updated`, and `Edit Permissions` opens `Edit User Permissions` with the permission matrix and `Updated`; strict local/reference comparison passed for clicked state, headings, table headers, PIN values, edit field values, and permission checkbox states using `reference-captures/emareez-staff-action-*.json` and `reference-captures/local-staff-action-*.json`. Staff permissions are enforced beyond storage: frontend filters sidebar routes, hides topbar Add New Sale when denied, redirects direct forbidden routes to the first allowed page, and backend middleware maps API path/method to the same permission keys before allowing non-owner/admin users. Live local API verification created temporary staff with only Dashboard + Sales History permissions; `/api/reports/sales-summary` returned 200, `/api/suppliers` returned 403 `Permission denied`, and cleanup deactivated the temporary staff. Chrome/CDP route probe `reference-captures/local-staff-permission-route-probe.json` logged in a restricted staff session, attempted `/pms/dashboard/supplier`, redirected to `/pms/dashboard`, removed Supplier/Add New Sale from visible text, then restored the browser to admin. Earlier temporary destructive workflow verification created a cashier with password, sales PIN, and permissions; confirmed duplicate email rejection; edited name/phone/address/role/gender/sales PIN/permissions; verified active search; soft-blocked the staff row; verified it disappeared from active listing and appeared with `is_active=false`; then hard-cleaned the temporary user. |
| Shift Management | Shift list and assignment | Done | Add/edit/delete UI implemented with server-backed search/pagination and real totals, count/card layout, structured off-days, staff display names, and AM/PM start/end controls; Assign Shift modal no longer exposes the extra Date field shown only by our backend model. Fresh local Chrome/CDP capture `reference-captures/local-shift-management-current-verified.json` verified the current E-Mareez-matching empty state with `Add Shift 0`, no tables/cards, no pagination/footer, no extra empty-state heading, and `No Data Found`. The same capture verified the Assign Shift modal visible controls match E-Mareez exactly: Search Name on the page, then Staff Member, Shift Type, Off Days, Start Time, AM, End Time, AM, Add, and `selectCount: 0`; hidden lookup IDs are not visible UI. Backend update validation accepts date edits instead of rejecting them as `None`; temporary local API verification created a shift, searched it by staff, edited shift type/time/off-days, deleted it, and confirmed totals returned to zero. A separate partial-update probe verified date-only edits preserve the existing shift type. |
| Expenses | Daily expenses and categories | Done | Expense and category add/edit/delete UIs implemented; date/date-range expense filters, list-level Download Expense PDF, per-row PDF download, and expense/expense-category search/pagination call the server with real totals and category display names. Captured the current E-Mareez Daily Expense table into `data/emareez-import/expenses-live.json` and imported all 17 rows locally with exact category mapping, including duplicate/case-sensitive category names such as `SALARY`; local ordering now matches E-Mareez by date descending and same-day capture order. Fresh Chrome/CDP verification using `reference-captures/emareez-expense-live-20260702.json`, `reference-captures/local-expense-live-20260702.json`, `reference-captures/emareez-expense-category-live-20260702.json`, and `reference-captures/local-expense-category-live-20260702.json` passed for Daily Expense initial headers/rows, Date Range blank From/To controls, `1-17 of 17` range footer, first range rows, `500 PKR` / `1,500 PKR` amount formatting, Add Expense title and input placeholders/types, Expense Category headers, first rows, `1-50 of 309` footer, Add Category title, Office Supplies input, Description textarea, and Add action. List-level expense PDF exports the complete filtered result set; direct generator verification produced an 80-row / 3-page PDF containing first row, last row, and `Total Expenses: 80`, and live API smoke test confirmed `/api/expenses/download-pdf` returns `%PDF-1.4` with Hassan Pharmacy, Daily Expense Report, and Total Expenses text. Expense PDFs use UI/reference labels (`Expense Category`, `Expense Amount`, `Description`, `Total Expense Amount`) and source address, customer-service phone, and license number from the editable pharmacy profile while keeping Hassan Pharmacy branding. Expense Category imports all 309 captured E-Mareez rows, including literal `- / -` rows, preserves reference ordering via `reference_sort_order`, renders empty descriptions blank, uses icon-only edit actions, and returns a controlled 409 when deleting an in-use category. Expense edit validation accepts date edits and partial payloads; temporary local API verification created an expense, edited date/name/amount, searched the edited row, deleted it, and confirmed cleanup. |
| Return Policy Notes | Manage return policy notes | Done | Policy and instruction add/edit/delete UIs implemented. Chrome/CDP verified captured table headers, policy text, instruction row order, Add Policy modal with Description textarea/Save, and Add Instructions modal with Title/Description textareas/Save. Backend return-note ordering now matches captured E-Mareez order. Fresh Chrome/CDP verification confirmed both policy and instruction rows now use blank visual action cells with SVG Edit/Delete icon buttons. |
| Change Password | Change current account password | Done | Staff/Admin toggle, three-password-field layout, and per-field password visibility toggles match the captured E-Mareez structure. Chrome/CDP verified Staff state with both captured headings, Admin state with account-password heading, hidden-by-default fields, Show/Hide toggle behavior, and Save action wiring. Live local API verification confirmed `/api/auth/change-password` is reachable from an authenticated session and rejects an incorrect current password with `400 Current password is incorrect` without mutating credentials. |
| Get Technical Help | Static support contact page | Done | Direct centered page layout, heading, phone/email links, dark left-arrow GO BACK TO DASHBOARD action, Hassan Pharmacy branding/contact replacement, and back navigation to dashboard verified in Chrome/CDP. |

## Reference Assets

- Latest full sidebar/page audit: `reference-captures/e-mareez-sidebar-audit/`.
- `page-summaries.json` contains extracted URLs, buttons, inputs, table headers, and modal summaries.
- `sidebar-groups.json` contains the expanded sidebar tree for Sales, Inventory, Daily Expense, and Settings.
- Reference captures also live in `reference-captures/` and `live-capture/`.
- Generated live table data used for imports lives in `data/emareez-import/`.
- Use those images/json files before changing a page for parity.
- Add new reference screenshots when an E-Mareez behavior is discovered.

## E-Mareez Reference Audit - 2026-07-01

Captured from the logged-in E-Mareez Chrome tab. Screenshots are named by sidebar order in `reference-captures/e-mareez-sidebar-audit/`.

### Sidebar Tree

- Dashboard
- Sales
  - New Sale
  - Return Item
  - Sales History
  - Return History
  - Product Sales History
  - Customer History
- Inventory
  - Batch
  - Demand/Order
  - Stock Audit
  - Order Purchase
  - Medical Products
  - Non Medical Products
  - Supplier
  - Category
  - Medicine Formula
  - Manufacturer
  - Stock Purchase
  - Shelf
- Staff Management
- Shift Management
- Daily Expense
  - Expense
  - Expense Category
- Settings
  - Change Password
  - Return Policy/Notes
- Get Technical Help

### Page And Control Map

| Sidebar page | Reference route | Primary controls | Table columns / visible behavior | Modal captured |
| --- | --- | --- | --- | --- |
| Dashboard | `/pms/dashboard` | Topbar Add New Sale, KPI cards, report panels | Dashboard metrics and expired batch/report panels | No |
| New Sale | `/pms/dashboard/newsale` | Tabs: New Sales, Recent Sales, Draft Sales; multiple New Sale workspaces; compact POS controls use a first row for Product/Service mode plus Customer Number/Name and a dedicated second row for Product Name, Quantity, Type, and Add; Product Name is capped at 560px and remains responsive so the entry controls stay compact | Viewport-filling Purchased Items table combines batch/shelf under Product and sale/total/remaining quantities under Quantity while retaining visible Cost Price and read-only calculated values. Per-row Edit/Done enables quantity, Rate/Sell Price, Discount %, and Discount Amount; draft Amount/Payable totals preview immediately, while checkout, draft saving, shortcuts, workspace switching, and other row actions wait for Done. Delete remains available on the active row, while product, batch, sale type, cost, and stock stay protected. Sticky table header, internal item scrolling, and pinned subtotal/discount/payable/draft/invoice bar keep entry and checkout controls on screen. Local 1440×687 verification fits seven complete rows with all four product-entry controls aligned on their dedicated row; large carts scroll only inside the table body. | Validation dialog when Add to List is clicked without fields |
| Return Item | `/pms/dashboard/return-item` | Original invoice input, explicit Search Invoice action with loading state, selectable Return Invoice Date, Generate Invoice | Batch no., Name, Quantity Sold, Quantity Returned, Rate/Sell Price, Amount, Action; invoice lookup uses one indexed request and selected return date is persisted to history/receipt | No |
| Sales History | `/pms/dashboard/saleshistory` | Date / Date Range radios, date/time filters, search by invoice/medicine, Print Sales History, rows-per-page, pagination | History table loads after filters/search; View opens Sales History Details and Print Invoice opens the saved invoice receipt preview | Sales History Details; Invoice Details; print option modal: Current Page Only, Entire Sales History, Current Page Summary, Entire Sales History Summary |
| Return History | `/pms/dashboard/returnhistory` | Select Date / Select Date Range radios, date filter, invoice search, All selector, rows-per-page, pagination | Invoice Number, Date, Time, Total Amount(RS.), Discount(%), Discount Amount, Total Payable, Paid, Due(s), Change Returned, Action | No |
| Product Sales History | `/pms/dashboard/productsaleshistory` | Select Date / Select Date Range radios, date filter, search, rows-per-page, pagination | Product Name, Dose, Generic Name, Sold Quantity, Action | View Product History modal with invoice rows and View / Print Invoice actions |
| Customer History | `/pms/dashboard/customer-history` | Search, rows-per-page, pagination | Name, Phone#, Due Amount, Actions | Edit Customer; Payment History with invoice rows, totals, payment methods, receive-now, Clear Dues, View and Print Invoice actions |
| Batch | `/pms/dashboard/batch` | Active/Reported tabs, Date/Date Range filters, In Stock filter, search, rows-per-page, Add New Batch | Batch No., Shelf No., Name, Medicine Formula, Stock In, Stock Out, Stock Remaining, Stock Purchase Price, Stock Purchase Price Before Tax, Sell Price, Cost Price, Total Cost, Production Date, Expire Date, Created at, Supplier Name, Paid Amount, Supplier Outstanding, Supplier Invoice no., Actions | Add Batch |
| Demand/Order | `/pms/dashboard/demand` | Supplier, medicine, type/quantity inputs, Add To List, search | Supplier Name, Product Name, Type, Quantity, Action/Actions | Validation dialog when supplier missing |
| Stock Audit | `/pms/dashboard/stock-audit` | Medicine search, quantity, adjustment selector, Add To List, SAVE | Product Name, Batch, Type, Quantity, Select Adjustment; audit list includes before/after quantities, adjustment type, amount, actions | Validation dialog when medicine missing |
| Order Purchase | `/pms/dashboard/order-purchase` | Medical / Non Medical radios, search, rows-per-page, pagination | Medicine Name, Dose, Total Quantity, R. Quantity, Manufacturer Name, Actions | No |
| Medical Products | `/pms/dashboard/medicines` | Active/Reported tabs, search, stock alerts, rows-per-page, Add New Medicine | Name, Formula, Dose, Total Quantity, Remaining Quantity, Generic Name, Category, Manufacturer Name, Actions | Add Medical Product |
| Non Medical Products | `/pms/dashboard/nonmedicines` | Active/Reported tabs, search, stock alerts, rows-per-page, Add New | Name, Brand Name, Category, Total Quantity, Remaining Quantity, Actions | Add Non Medical Product |
| Supplier | `/pms/dashboard/supplier` | Search, rows-per-page, Add New Supplier, supplier-name drill-down | Supplier Name/Company, Contact Person, Phone#, Email, Total Batches, Stock Purchase Price, Paid Amount, Supplier Outstanding, Status, Actions | Add New Supplier |
| Supplier Details | `/pms/dashboard/supplier/details?supplier_id=...` | Back to Supplier, invoice drill-down | Supplier Invoice No., Batches, Total Stock, Remaining Stock, Total Cost, Expected Sale Value, Expected Profit, Latest Batch Date, Actions | Click invoice |
| Supplier Invoice | `/pms/dashboard/supplier/invoice?supplier_id=...&invoice_no=...` | Back to Supplier Details | Batch No., Name, Total Boxes, Pattas/Box, Goli/Patta, Total Stock, Remaining Stock, Cost Price, Sale Price, Expected Sale Value, Expected Profit, Expire Date | No modal |
| Category | `/pms/dashboard/category` | Search, rows-per-page, Add New Category | Category Name, Type, Actions | Add New Category |
| Medicine Formula | `/pms/dashboard/medicineformula` | Search, rows-per-page, Add New Formula Name | Medicine Formula Name, Description | Add New Formula Name |
| Manufacturer | `/pms/dashboard/manufacturer` | Search, rows-per-page, Add New Manufacturer | Manufacturer Name | Add New Manufacturer |
| Stock Purchase | `/pms/dashboard/purchases` | Search, rows-per-page, pagination | Batch No., Medicine Name, Quantity, Rate, Total Amt, Extra Discount/Bonus, Purchase Date, Stock Cost Price, Sales Tax, Invoice ID, Expire Date, Supplier Name | No |
| Shelf | `/pms/dashboard/shelf` | Search Medicine, rows-per-page, Add New Shelf | Name, Total Batches, Actions | Add Shelf modal uses one Shelf Name field with Shelf A placeholder and Add action; batch quick-add shelf flow uses the same labels |
| Staff Management | `/pms/dashboard/staff-management` | Search Name, Add Staff | Name, Role, Phone #, Address, Email, Actions | Add Staff modal now matches the captured E-Mareez three-step flow: step 1 roles include Pharmacist/Technician/Assistant/Manager/Intern/Stock_manager/Cashier and Gender only Male/Female; step 2 has six PIN inputs with Next only; step 3 has Select All Permissions plus the 17-row View/Edit/Add/Delete permission matrix with 40 initially unchecked boxes and Back/Done actions. Chrome/CDP captured `emareez-staff-add-modal-step3-real-full.json` and verified local `local-staff-modal-step3-after-permission-patch.json`. |
| Shift Management | `/pms/dashboard/shift-management` | Search Name, Add Shift | Shift cards/list with count | Assign Shift |
| Expense | `/pms/dashboard/daily-expense` | Date / Date Range radios, date filter, Download Expense PDF, Add Expense | Date, Name, Expense Category, Expense Amount, Actions | Add Expense modal matches captured labels/placeholders: Expense Name, Date, Expense Category, Amount, Description, Add |
| Expense Category | `/pms/dashboard/expense-category` | Search Category, rows-per-page, Add New Category | Category Name, Description, Actions | Add Category modal matches captured title/placeholders: Office Supplies and Enter Description... |
| Change Password | `/pms/dashboard/settings/change-password` | Staff/Admin toggle, current/new/verify password with visibility toggles, Save | Authenticated password-change endpoint with current-password validation | No |
| Return Policy/Notes | `/pms/dashboard/return-policy` | Add New Policy, Add New Instructions | Policy Description, Actions; Title, Description, Actions | Add Policy modal uses Description textarea and Save; Add Instructions modal uses Title/Description textareas and Save |
| Get Technical Help | `/pms/dashboard/technicalhelp` | Static contact/help page | Heading: For any Query Contact Hassan Pharmacy Care; phone, email, and dark left-arrow GO BACK TO DASHBOARD action | No |

### Captured Modal Field Sets

| Modal | Fields / behavior captured |
| --- | --- |
| Add Batch | Supplier Invoice No.; Supplier; Name; Total Boxes; Units per box; Items per unit; Total Stock; Cost Price; Sell Price; Shelf; Expire Date. Product type is inferred from the selected product; an internal batch number is generated for new rows because the API still requires one. |
| Add Medical Product | Bar Code, Name, Medical Formula, Category, Dose, Unit, Generic Name, Manufacturer Name, prescription checkbox. |
| Add Non Medical Product | Bar Code, Name, Brand/Manufacturer Name, Weight, Unit, Category. |
| Add New Supplier | Supplier Name/Company, Contact Person, Phone#, Email. |
| Add New Category | Type, Category Name. |
| Add New Formula Name | Name, Description. |
| Add New Manufacturer | Manufacturer Name. |
| Add Staff | Multi-step: Add Staff, Assign Pin, User Permissions. First step fields: Name, Role, Email, Number, Address, Gender, Sales PIN. Second step: Create 6 Digit Pin for your staff member with six one-character inputs. Third step: User Permissions. |
| Assign Shift | Staff Member, Shift Type, Off Days, Start Time with AM/PM, End Time with AM/PM. |
