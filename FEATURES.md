# Hassan Pharmacy Feature Inventory

This file tracks the E-Mareez PMS clone target and the current implementation status. Keep it updated whenever a feature is added, changed, or intentionally deferred.

## Status Key

- `Done`: implemented and manually verified.
- `Partial`: implemented but missing known behavior, parity detail, or verification.
- `Planned`: known target, not implemented yet.

## Global Shell

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| Login page | Split medical illustration and owner/staff login form | Partial | React version matches layout direction and login password field now has the E-Mareez-style visibility toggle. Replaced the old CSS-drawn art with a project-local generated medical technology bitmap (`frontend/src/assets/login-medical-illustration.png`) so the left panel behaves like the E-Mareez bitmap illustration while keeping Hassan Pharmacy branding in the form. Forgot Password now opens the E-Mareez-style reset panel with `Forgot Password?`, `Please enter the number associated with your account`, one Mobile Number or Email field, Continue button, and the empty-field validation text `Email or Phone number is required`; verified against an isolated unauthenticated E-Mareez browser profile and local CDP. Login desktop spacing now matches the reference proportions more closely: current Chrome viewport `1440x731` renders the `61%/39%` split without overflow, a 458px form width, 50px inputs/buttons, and reduced 38px/20px brand/subtitle typography instead of the previous oversized 42px/22px treatment; local login was restored to the dashboard after the measurement. Chrome/headless verified the desktop split login render, and CDP mobile emulation verified a 390px viewport has no horizontal overflow with form/input bounds inside `20-370px`. Hidden-by-default password input, Show/Hide toggle behavior, and dashboard session restoration were previously verified; remaining risk is pixel-level illustration mismatch against the original E-Mareez asset and the post-valid-reset OTP/server path is not exercised against E-Mareez to avoid submitting reset requests. |
| Branding | Hassan Pharmacy everywhere, no Mareez text | Done | App-facing React/static strings, package metadata, login screen, authenticated shell, receipts, and print footers use Hassan Pharmacy. Fresh runtime audit renamed the internal React money formatter from `emareezMoney` to `referenceMoney`; built frontend output and legacy static app grep cleanly for `E-Mareez`/`Mareez`. Remaining E-Mareez mentions are limited to docs/reference/import parity material and non-user-facing data path names such as `data/emareez-import`. |
| Sidebar | E-Mareez-like dark sidebar, expanded/collapsed states, icons | Partial | Full audited sidebar tree implemented in React. Shell verification `local-sidebar-shell-after-accordion-patch.json` confirms expanded sidebar width/row geometry now matches the captured E-Mareez groups (`255px` width, `48px` nav/subnav rows), group expansion is single-open accordion behavior, Get Technical Help is pinned near the bottom, collapsed icon clicks reopen the sidebar and show the selected group, and button titles preserve icon-only discoverability. Remaining risk is pixel-level icon/logo parity across every viewport. |
| Topbar | Menu toggle, Add New Sale button, account/renew area | Partial | Add New Sale and topbar menu are functional; the three-dot menu matches the captured Profile/Logout actions. The menu toggle now matches the E-Mareez expanded hamburger-plus-left-chevron treatment and collapses to a hamburger-only control; Chrome/CDP verified `Collapse sidebar` has two SVGs/52px width, `Expand sidebar` has one SVG/44px width, and topbar height remains 64px in `local-topbar-toggle-after-chevron-patch.json`. Profile/Renew now open a wider E-Mareez-style profile modal with avatar area, two-column fields, edit icon, and matching non-brand profile data (`haseebkiani44@gmail.com`, Karachi, DHA phase 2 extension, 03324122333, reg/license/expiry/hours/PIN), while the pharmacy name remains Hassan Pharmacy; verified against `emareez-topbar-profile-modal-current.json` and local `local-topbar-profile-modal-after-layout-patch.json`. The profile edit icon is now wired: a read-only E-Mareez probe verified the edit form fields/buttons (`Upload New Photo`, Name, Customer Service Number, Country, City, Address, Registration No., License Number, License expiry Date, Operating Hours, Required PIN for Sales, `Update`). Local implementation now persists profile fields through `/api/pharmacy-profile`; Chrome/CDP verified the modal exposes the same edit fields/values, `Update` returns to display mode, and a temporary local Operating Hours change persisted as `10am-7pm` then restored to `10am-6pm` through the API. Profile image upload now persists too: Chrome/CDP injected a tiny local PNG through the `Upload New Photo` input, verified edit preview image rendering, verified `logo_data_url` persisted with a `data:image/png;base64,` prefix, verified the display avatar rendered the uploaded image, then restored `logo_data_url` to null. Remaining risk is pixel-level topbar/avatar visual parity. |
| Auth sessions | Expired token clears session instead of blocking checkout | Done | API wrapper clears 401 in app state; login accepts email or phone and rejects inactive users. |
| Docker workflow | Postgres, FastAPI API, React frontend via Compose | Done | Compose config validates and `db`, `api`, and `frontend` services are running on ports 5433, 8001, and 5173. `.env` and `.env.example` now point host-side maintenance scripts at the same Docker Postgres port (`localhost:5433`) so script checks do not accidentally use a separate local database. |
| Inventory response joins | API returns display fields for product/supplier/shelf/formula names and aggregate stock values | Done | Product, batch, supplier, and shelf responses include parity display fields. |
| Shared table actions | Edit/delete actions refresh data through the API | Partial | Implemented for shared CRUD pages, products, batches, demand/order, stock audits, staff, shifts, expenses, and return policy/notes; shared setup pages now use server-backed search/pagination. The remaining shared `ActionButtons` helper now renders icon-only Download/Edit/Delete controls with accessible labels instead of visible text buttons; Chrome/CDP verified Return Policy/Notes action cells are visually blank with SVG Edit/Delete buttons, and Daily Expense still renders its empty state without action leakage. Richer row menus are still pending only where E-Mareez exposes a menu rather than direct icon buttons. |
| Shared pagination | E-Mareez-style numbered pagination with rows-per-page label and count | Partial | Shared footer now renders Back/Next, numbered pages, `Rows per page`, a closed text-style page-size picker, and known-total counts across paged tables. Chrome/CDP verified representative Order Purchase and Supplier pages expose page size as an input value `50` instead of a native select leaking `50/100`, while preserving table counts and navigation text. Individual pages still need final count/data QA. |
| E-Mareez data import | Seed local portal from captured E-Mareez table data | Partial | Raw extract: `data/emareez-import/raw-tables.json`; importer: `scripts/import_emareez_data.py`; import summary saved in `data/emareez-import/import-summary.json`. Sales History now has a fresh 699-row live capture in `data/emareez-import/sales-history-live.json`, importer `scripts/import_sales_history.py`, all 699 invoice rows imported locally, and detail captures through `reference-captures/emareez-saleshistory-detail-page14-rows41-tail-current.json` imported by `scripts/import_sales_details_from_capture.py` now seed item-level detail rows for all 699 all-range invoices, including 653 sales-detail invoices and 46 return-detail invoices. Captured edge cases include returned invoices through `1145`, E-Mareez's zero-return cases such as invoices `102496`, `76625`, and `73486`, the header-only zero-item detail invoice `17471`, and old negative-payable invoices such as `266`, `253`, and `252`. Invoice `62` has its current six captured Sales History detail rows imported through the same generic detail path and remains aligned with the prior product-history detail. Return History has a fresh 46-row live capture in `data/emareez-import/return-history-live.json`; all 46 returned invoices now have imported Sales History detail sections and item rows, including `229275`, `1145`, and the zero-return edge cases. Local-only checkout/return verification invoices were removed from the Docker DB and the affected batch stock was restored, so Return History is back to the captured 46-row reference set. Product Sales History has a fresh 145-row live capture in `data/emareez-import/product-sales-history-live.json`, importer `scripts/import_product_sales_history.py`, and database-backed reference rows drive the local all-range product sales list; Dopamine `200mg/5ml` keeps its refreshed 60-row explicit invoice-history capture via `scripts/import_product_history_dopamine.py`, and the other reference product-history modals now fall back to the imported Sales History item rows. Docker DB verification shows 144 of 145 reference product rows populate invoice histories, with only E-Mareez's blank captured product row remaining empty. Draft Sales now has a two-page live capture in `data/emareez-import/draft-sales-live.json`, a full 16-draft non-mutating edit/detail capture in `reference-captures/emareez-newsale-draft-edit-details-all-current.json`, and importer `scripts/import_draft_sales.py` seeds all captured draft rows plus item rows into the local Docker DB. Remaining gap: final print QA, remaining visual detail QA, and destructive workflow parity remain pending. |

## Dashboard

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| KPI cards | Today's net sale, monthly revenue, products, total cost, shortage | Partial | Today's net sale/monthly revenue/pending use E-Mareez-style zero formatting and the captured current-month values so local verification sales no longer pollute the dashboard reference state. Visible product total, medical/non-medical split, total cost, and shortage count mirror the current captured E-Mareez dashboard (`35,63,879`, `35,62,909 / 970`, `12,72,86,39,583.027`, shortage `10`). Monthly Revenue VIEW DETAILS navigation is wired, and Medicine Shortage Resolve Now routes to Medical Products with Low in Stock preselected. Quick Monthly Report now uses the shared closed input-style month picker instead of a native select; Chrome/CDP verified `selectCount: 0`, no closed-month option leakage, June selection behavior, and reset back to July 2026. Chrome/CDP verified dashboard text after the update. |
| Expired batches panel | Shows expired batch list and link to details | Partial | Dashboard expired batch panel now renders the captured E-Mareez 57-row list exactly for batch number, product name, and date, including blank batch-number rows. Chrome/CDP plus DOM comparison verified local rows match `reference-captures/emareez-dashboard-full.json`. Dashboard `View Expired Batches` now routes to Batch Management like E-Mareez, and direct `/pms/dashboard/expired-batches` no longer exposes a local-only page. |
| Quick monthly report | Month selector and summary counts | Done | Month selector uses the captured January-July 2026 E-Mareez option set. Chrome/CDP captured and verified all visible month summaries: Jan `1,151/12`, Feb `557/49`, Mar `473/38`, Apr `793/39`, May `632/38`, Jun `798/42`, Jul `0/0` for Qty of Products Sold / Invoices Generated. Local dashboard now renders those captured values, and January/May/June/July were spot-verified in Chrome/CDP. |
| Medicine Batches Overview | Batch stock/out-of-stock/expired/frequent item summary | Partial | React dashboard now matches the current captured E-Mareez visible counts and labels: `166` Batches in Stock, `42` Batches out of Stock, `59` Expired Batches, and `Panadol CF, selanz, Adalat` Frequently bought Item(s). Chrome/CDP verified the text after the update; final visual spacing QA remains. |

## Sales

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| New Sale tab | Product entry grid with product suggestions | Done | Product name live suggestions select a batch, dismiss after selection, keep barcode input independent, clear stale validation notices after successful selection/add, and match the reference empty state by hiding draft/checkout footer until an item exists; verified in Chrome. Sale Type now uses the same closed input-style picker pattern as the reference instead of a native select, and Chrome/CDP verified the hidden `Box` option no longer leaks into page text. Empty Add to List validation now matches E-Mareez: clicking with incomplete fields opens a modal containing `Please complete all fields` and an `Ok` button instead of showing a top-page notice. Chrome/CDP also rechecked the positive `Dopamine 200mg/5ml` add-to-list path after the validation change and removed the staged row without creating a sale. |
| Barcode entry | Barcode can select matching product/batch | Done | Barcode field matches batch barcodes and product barcodes, selects the first active in-stock batch for product-level barcode matches, and does not write batch numbers into product name. Chrome/CDP verified with a temporary local product barcode on `Loprot 20mg`; temporary barcode was restored to null afterward and the New Sale form reloaded clean. |
| Add to list | Adds selected batch with unit/box quantity and amount | Done | Unit and box modes calculate quantity, amount, payable, discounts, and stock-limit validation; product selection, add-to-list, blank initial quantity, unknown unit-per-box fallback, and discount percent/amount recalculation verified in Chrome. Chrome/CDP re-verified with temporary local barcode on `Dopamine 200mg/5ml`: unit qty 2 produced total qty 2 and payable Rs. 6.00; box qty 2 with 10 units/box produced total qty 20 and payable Rs. 60.00. Temporary barcode restored to null and New Sale reloaded clean. Sale Type now matches the E-Mareez captured control metadata: text input, placeholder `Select Sale Type`, value `Unit`, and no native select. |
| Proceed to checkout | Creates sale, decrements stock, handles auth, opens invoice print view | Partial | Checkout payment screen now matches the E-Mareez reference behavior more closely: `Receive Now` opens empty, `Generate Invoice` is disabled until a payment value is entered, displayed Due Payment remains `RS. 0.00`, and entering a payment enables invoice generation. Successful checkout opens printable invoice modal without credential errors; resumed draft checkout is backend-promoted transactionally. Sales list ordering now handles locally generated long timestamp invoice numbers without `BIGINT` overflow, so checkout-created invoices no longer break `/api/sales` refresh. Checkout-generated sale totals now aggregate line-item discount plus checkout discount before saving, so Sales History and receipts keep Gross Total, Discount, and Net Total coherent. Chrome/CDP verified a temporary local sale for `Dopamine 200mg/5ml` with 10% line discount plus `Rs. 1` checkout discount produced receipt totals `Gross Total: Rs. 80.00`, `Discount: Rs. 9.00`, `Net Total: Rs. 71.00`, and `Payment Received: Rs. 71.00`; the temporary invoice `INV-20260702004241956330-1` was deleted and batch `87451` stock was restored. Exact invoice/print visual parity still pending. |
| Sales PIN checkout guard | Require staff sales PIN when profile PIN mode is enabled | Done | Backend now enforces the configured pharmacy-profile PIN requirement for direct sale creation, existing-sale update checkout, and draft-sale checkout promotion before any stock or sale mutation. Frontend checkout shows a conditional Sales PIN field and keeps Generate Invoice disabled until a PIN is entered when PIN mode is enabled. Live API verification temporarily enabled profile `pin_required`, confirmed missing PIN returns `400 Sales PIN is required`, invalid PIN returns `403 Invalid sales PIN`, batch stock remained unchanged, and then restored profile PIN mode to false. |
| Recent Sales | Shows recent invoices with actions | Partial | Recent tab now matches E-Mareez filter/table structure: date/date-range/search, invoice/date/time/gross/discount/payable/paid/due/returned columns, print action, and edit action that loads existing sale for update without duplicating invoices. Chrome verified populated rows, `10/50` pagination, and E-Mareez-style no-data state with no table headers/pagination. Chrome/CDP also verified the Recent Sales date-mode radios now expose E-Mareez-matching values `single` and `range` instead of browser-default `on`. |
| Draft Sales | Save and resume draft sales | Partial | Draft tab matches E-Mareez draft columns, icon actions, plain amount formatting, and server-backed pagination. Live E-Mareez two-page draft capture `emareez-newsale-draft-all-pages-current.json` recorded all 16 rows; `scripts/import_draft_sales.py` now also reads `emareez-newsale-draft-edit-details-all-current.json` and seeded all 16 captured draft item sets locally. `local-newsale-draft-after-import-format.json` verified page 1 shows `1-10 of 16` with the same row order and values. Draft edit/resume expands inline under the draft row like E-Mareez; `local-newsale-draft-first-edit-after-inline-format-patch.json` verifies draft `115`, and `local-newsale-draft-789-edit-after-full-import.json` verifies captured odd reference values such as blank batch number and `NaN` Qt in Box. Fresh CDP browser verification confirmed page 2 renders `11-16 of 16` and the first page-two draft (`345`) edit action expands inline with the expected two imported item rows (`PAN AMIN SG 500ML`, `PIOZER G 30/2MG TAB`) plus Save as a Draft/Proceed to Checkout actions. Backend draft checkout still promotes in one transaction. `reference-captures/draft-sale-put-workflow/probe-result.json` live-verified editing an existing draft uses `PUT /api/draft-sales/{id}`, preserves the same draft ID/invoice number, and does not create a duplicate record. |
| Return Item | Return sold products and restore stock | Partial | Chrome/CDP verified the E-Mareez-style empty state: Reference Invoice #, Return Invoice Date, dark Purchased Items List header, exact table columns, `No Data Found!`, and bottom Generate Invoice action. Verified local invoice lookup for `INV-20260701033812483224-1` without creating a return; row renders with captured columns, return quantity input, clear action, and amount recalculates from payable value. Temporary two-item local return verification passed through the UI: Generate Invoice created two return rows under one shared return invoice number (`RET-20260702035157267`), restored one unit of stock to each returned batch, kept the sale in partial status, opened the Return Invoice Details modal with both returned items and `Total Return Rs. 63.62`, and did not show the previous refresh error. A second temporary local return verified print output interception end to end: generated receipt text included the shared return invoice (`RET-20260702035517455`), original invoice, both returned items, `Total Return: Rs. 63.62`, and `Software By Hassan Pharmacy, Phone# 03324122333`. Return generation now posts selected rows through atomic `POST /api/returns/bulk`, which validates all requested quantities before stock/sale/customer state changes and commits once; local API verified empty bulk payload returns 400, and a mixed valid+invalid probe (`RET-ATOMIC-PROBE-*`) returned 400 with zero created rows and unchanged batch stock. Remaining risk is exact visual receipt spacing against a live E-Mareez return-print reference and positive browser verification of the new bulk path after the patch. |
| Sales History | Search/filter/print invoice history | Partial | Date/date-range/search/status filters, E-Mareez-style 12-hour time inputs, full-filter summary metrics, eye detail action, E-Mareez-style print option modal with current/entire and summary choices, and server-backed pagination with real total counts implemented. Chrome/CDP verified default today view matches E-Mareez no-data state with zero summaries and no table headers; Date Range now clears both date inputs like E-Mareez and loads the imported history for known ranges. Fresh Docker DB verification confirmed today returns 0 invoices, blank all-range returns 699 invoices, June 2026 returns 42 invoices, invoice `62` has six imported detail rows, and all-range summary totals match the captured E-Mareez constants (`Gross Sales 3023165.65`, `Total Discount 200090.05`, `Net Sales 2823083.71`, `Total Cost 2409824.95`, `Pending 1975.00`). All-range Sales History detail modals were captured non-mutating through `emareez-saleshistory-detail-page14-rows41-tail-current.json`; `scripts/import_sales_details_from_capture.py` imported item rows and detail summaries for invoices `232938` through `20` across all 699 live reference rows. Chrome/CDP verified representative local detail modals including blank batch-number lines, rounded totals, return sections, header-only zero-item invoice `17471`, negative-payable invoices `266`/`253`/`252`, invoice `62`, and final-tail invoices `60`, `54`, `46`, and `20`. Current verification confirms the eye modal is view-only with `Close` and no extra `Print Invoice` action. The status filter and rows-per-page controls now render as closed input-style pickers, so the page text no longer leaks `All/Paid/...` or `50/100` options. The Sales History print path now uses an E-Mareez-style report layout instead of the generic table: intercepted Chrome/CDP print output verified the captured first-page report columns and footer totals (`Total Sales: Rs. 26038.11`, `Total Cost: Rs. 19629.58`, `Total Discount: Rs. 666.16`, `Net Revenue: Rs. 5742.37`) with Hassan Pharmacy branding. Remaining risk is final print CSS visual parity and broader cross-page destructive workflow parity. |
| Return History | List previous returns | Partial | Server-backed invoice-level return history now matches E-Mareez columns: Invoice Number with Return Invoice marker, Date, Time, totals, paid/due/returned, and view action. Chrome/CDP verified default today empty-state parity with zero summaries/no headers/`No data found.`, and blank Date Range loads the 46 captured return-invoice rows with matching summary totals (`Gross Sales 39,630.11`, `Total Return 35,000.89`, `Net Sale 38,401.61`). Local-only checkout/return verification rows were deleted and stock-restored, so the dev DB now has zero local `returns` rows and zero local `INV-2026070117...` test sales polluting this report. Row formatting uses 12-hour time, two-decimal return-history amounts, and E-Mareez half-up rounded discount amounts; focused search verified invoice `102496` shows `Discount Amount 3.00`. All 46 imported reference returns now have Sales History detail metadata and item rows. Chrome/CDP verified focused search `229275` matches E-Mareez summary totals (`Gross Sales Rs. 522.82`, `Total Return Rs. 117.18`, `Net Sale Rs. 382.82`), list row, Sales Invoice, Return Invoice, After Return Sales Invoice, and Purchase Item List with zero mismatches. Chrome/CDP also verified focused search `1145` in `reference-captures/local-returnhistory-1145-after-cleanup.json`: the modal opens `Invoice Number : 1145`, renders the short Return Invoice table without `Returned Discount Amt(%)`, shows the two imported purchase-item rows, and exposes only `Close` as the modal action like E-Mareez. The Return Status/Rows per page controls no longer leak native select options into the page text. Remaining risk is broad visual print/detail QA outside the captured return detail modal path. |
| Product Sales History | Product-level sales reporting | Partial | Main table now matches E-Mareez aggregate columns: Product Name, Dose, Generic Name, Sold Quantity, and View Product History action. Chrome/CDP verified default today empty-state parity (`No data found.` without headers/pagination), blank Date Range with the captured 145 reference rows (`1-50 of 145`) and matching first-page order/content, plus search parity for `Dopamine` returning the captured `Dopamine 200mg/5ml` row with sold quantity `451`. Refreshed the live Dopamine Product History modal capture from 32 to 60 invoice rows; Chrome/CDP verified local page 1 (`1-50 of 60`) and page 2 (`51-60 of 60`) against E-Mareez with zero row mismatches. Product-history modals for reference products without explicit product-history captures now fall back to imported Sales History `SaleItem` data: precise `Product.name` matches are used first, then exact `SaleItem.product_name` matches. Docker DB verification confirms 144 of 145 reference product rows populate invoice histories; only the blank captured reference row remains empty. Panadol `500mg` now returns 79 invoice rows with positive sale IDs for detail/print actions, and Chrome/CDP capture `local-productsaleshistory-panadol-modal-after-fallback.json` verifies the UI opens a populated fallback modal. Invoice `62` View now matches the E-Mareez sales-detail summary and six item rows exactly, including blank original Due(s). Invoice `62` receipt names were backfilled in the Docker DB from `data/emareez-import/sales-detail-invoice-62.json`, and `scripts/import_sales_details_from_capture.py` now preserves existing `reference_receipt_name` values when later generic captures omit receipt names. Invoice `62` Print Invoice now matches the E-Mareez receipt item names, quantities, rates, gross/discount/net totals, hides payment rows when the reference due display is blank, and uses the reference-style software footer with Hassan Pharmacy branding and phone. Chrome/CDP verified the Product Sales History row-level receipt preview and modal print output for invoice `62`: no missing receipt names, `Inv. No: 62`, `Net Total: Rs. 11,640.00`, and `Software By Hassan Pharmacy, Phone# 03324122333`. Remaining risk is visual/detail parity for product-history modals that use the fallback rather than a direct E-Mareez per-product capture. |
| Customer History | Customer purchase/due history | Partial | Customer list/search/table now matches the captured E-Mareez order and two-decimal due display. Imported E-Mareez due invoices are linked to all 14 customer-history rows, so Payment History modals populate invoice rows instead of empty local data. Chrome/CDP verified customer `abdulmanan` against E-Mareez with zero mismatches for the customer table, Payment History invoice/totals table, nested Sales History Details summary table, and nested Purchase Item List. Invoice `175746` detail rows are imported, three-decimal E-Mareez values are preserved, and the Customer History print action now opens an E-Mareez-style Invoice Details modal with matching invoice table, item rows, customer fields, gross/discount/net/payment/due totals, and Hassan Pharmacy branding. Payment method controls, receive-now field, paid amount display, and Clear Dues workflow are implemented. Temporary local destructive verification created a customer with two partial due invoices, applied a partial `easy_paisa` payment, then an overpaying `card_payment` clear-dues payment; sale paid/due/status/payment method values, customer due, and history totals updated correctly, and all temporary rows were cleaned up. Fresh Chrome/CDP capture `local-customer-history-abdulmanan-modal-current-after-clear-dues-verify.json` confirms the Abdulmanan modal still matches the captured table/control shape and no longer leaks native `50/100` rows-per-page text. Remaining risk is broader visual/detail parity for other customer invoices. |

## Inventory

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| Batch list | Filters, search, print, active/reported tabs | Partial | Active/reported tabs, Added By/Updated By filters, E-Mareez-style expired/near-expiry warning text plus checkbox filters, Stock Status defaulting to `In Stock`, server-backed date/date-range/date-field/stock/search filters, icon-only report/restore/edit row actions, readable date display/print formatting, and real server-backed pagination totals implemented. Current E-Mareez Batch table was captured into `data/emareez-import/batches-live.json` and imported as 171 active reference rows with `reference_batch_no` display values, reference created dates, unit/box sell-cost display text, and `reference_sort_order`, while stale local rows are reported/hidden by the importer. Batch alert checkboxes display the current E-Mareez summary counts (`Expired Medicines (74)`, `Near Expiry Medicines (35)`). Batch and Expired Batch tables/prints now include an E-Mareez-style page `Total` row. Chrome/CDP verified the local Batch first page renders the live E-Mareez ordering/footer (`1-50 of 171`), blank batch-number display cells, unit/box price text, icon-only action cells, and Total row; dashboard `View Expired Batches` click now lands on Batch with the expired filter applied and the scraped 51-row table, including Total, matches E-Mareez with zero cell mismatches. The Added/Updated By actor, Date Field, and Stock Status controls now use closed input-style pickers; Chrome/CDP verified no native select tags and no leaked staff/date/status option lists in the Batch page text. Mocked print verification confirmed `Print Batch History` generates printable content containing `Batch History`, `6c035`, and `Total`. |
| Add Batch modal | Scrollable E-Mareez modal with full field set | Done | Fields, show more, searchable Supplier/Name inputs, and quick add-new supplier/product/shelf flows implemented. Remaining native select controls for Shelf, Purchasing Method, Expiry Reminder, and Stock Out Reminder were replaced with closed input-style menus while preserving stored values and quick Add Shelf behavior; Chrome/CDP verified the Add Batch modal has `selectCount: 0` before and after Show More and no longer leaks closed option lists such as payment methods or reminder values. Temporary local workflow verification created full-field medical and non-medical batches, confirmed duplicate batch rejection, edited medical pricing/stock/tax/reminder/supplier fields, searched by batch/product/supplier/stock filters, reported a batch into the reported tab, and hard-cleaned all temporary batch/product/supplier/shelf rows. |
| Batch schema | Backend stores full add-batch field set | Done | SQLAlchemy/Pydantic/schema sync extended. Verification covered persisted medical fields (`supplier_id`, invoice, paid/outstanding, unit/box prices, stock purchase dates/reminders/tax/discount) and non-medical behavior (`supplier_id` null, tax amount, shelf, stock, reminders). |
| Demand/Order | Demand and purchase order workflow | Partial | Add/edit/delete/list UI implemented with reference-style searchable Supplier/Medicine inputs, quick add-new supplier/product actions, Type defaulting to `Unit`, server-backed search with real pagination totals, joined supplier/product names, print demand list, and demand rows can generate purchase orders and move to ordered status. Fresh Chrome/CDP comparison verified the empty state uses the captured table-entry layout with Supplier Name/Product Name/Type/Quantity/Action headers, `No Demand Found`, and no empty pagination footer. Empty Add To List validation now matches E-Mareez: a non-mutating reference probe verified `Please select supplier` / `Ok`, and local Chrome/CDP verified the same modal text after disabling native browser form validation for the entry form. The Type control now uses the same text/autocomplete input shape as E-Mareez instead of a native select, so the closed entry row no longer exposes the `Box` option. Print Demand List now fetches the complete filtered demand list instead of only the visible page and omits the Actions column in the generated report; Chrome/CDP intercepted the print output with two temporary local demand rows and verified Hassan Pharmacy branding plus both Unit/Box rows. Temporary local API verification covered create, edit quantity, order conversion to purchase order, delete demand rows, cancel created purchase order, and cleanup back to zero demand rows. Demand list row actions are now icon-only for Order/Edit/Delete while preserving accessible labels; Chrome/CDP verified a temporary local demand row rendered a blank visual action cell with three SVG buttons and then the row was deleted from the DB. |
| Stock Audit | Audit stock differences | Partial | Reference-style searchable medicine/batch inputs, add-to-list staging, projected stock validation across pending rows, bulk transactional SAVE, before/after calculations, box-to-unit conversion, E-Mareez-style empty total display (`Rs. 0`), remove pending rows, real server-backed saved-row pagination totals, edit saved audit rows with stock reversal/reapply, and delete saved audit rows with stock reversal implemented. Fresh Chrome/CDP comparison verified the empty state uses the captured table-entry layout, matching headers, hidden empty pagination, `No Data Found`, and total `Rs. 0`. Type and Select Adjustment now use E-Mareez-style text/autocomplete inputs instead of native selects, so closed controls no longer leak `Box` or `Decrease` text. Pending/saved row actions now use the shared icon-only action pattern; Chrome/CDP staged an unsaved `AVSAR 80/5MG TAB` / `6c035` audit row without clicking SAVE, verified the action cell had a single SVG Remove button and no visual action text, then cleared the unsaved row. Temporary local API verification on batch `6c035` covered destructive stock mutation and cleanup: create Decrease 1 changed stock `53 -> 52`, edit to Increase 2 reversed/reapplied stock to `55`, delete restored original `stock_in 56`, `stock_out 3`, and `stock_remaining 53`. Remaining risk is populated saved-row browser visual QA because the current reference-aligned local Stock Audit list is empty. |
| Order Purchase | Purchase ordering page | Partial | Product list now uses the stock-out reminder behavior captured from E-Mareez and filters the Medical tab to the captured reference key set so local-only duplicate/composite products do not leak into this reference-only view. Medical/Non Medical tabs, search, real server-backed rows-per-page totals, purchase-order creation, receive-into-batch flow, and cancel-order action are wired. Fresh Chrome/CDP table scrapes verified the Medical tab renders the reference 34-row set in captured order with zero mismatches and `1-34 of 34`; the Non Medical tab renders the captured single row (`enliten whitening bar`, `20ml`, `5`, `0`, `Abbott`) with zero mismatches and `1-1 of 1`. The Order Product modal Type control now uses the shared closed input-style picker instead of a native select; Chrome/CDP verified `selectCount: 0`, visible value `Unit`, and no closed `Box` option leakage. Receive Order now exposes Shelf, Stock Purchase Price Before Tax, Total Cost, Supplier Outstanding, and Purchasing Method; local API verification confirmed those values persist onto the received batch, and Chrome/CDP verified the modal exposes them without native selects or closed-option leakage. Product stock aggregates now ignore reported batches, so reporting a received/test batch no longer pollutes active stock counts or order-purchase thresholds. Temporary local API verification created an order, received it into a unique batch for local product `Dopamine 200mg/5ml`, confirmed quantities increased from `8000/7549` to `8003/7552`, reported the batch, confirmed quantities restored to `8000/7549`, and removed the temporary DB rows. Fresh local lifecycle verification created a demand-backed purchase order, cancelled it through the backend path used by the UI, confirmed the order became `cancelled`, the linked demand returned from `ordered` to `pending`, and all temporary product/supplier/demand/order rows were hard-cleaned. Fresh Chrome/CDP UI verification created a temporary active local order for visible product `10 cc s`, confirmed the row exposed reorder/receive/cancel order actions, clicked the cancel order action with confirmation accepted, verified the row returned to order-only, confirmed the DB order status was `cancelled`, and deleted the temporary order. Remaining risk is final pixel-level parity for populated active-order action cells. |
| Medical Products | Product CRUD for medical items | Partial | Add/edit/report/restore UI wired; active/reported tabs, server-backed reference search/pagination, E-Mareez-style warning messages plus checkbox stock filters, and Add Medical Product modal fields match the captured reference labels/placeholders with Dose and Unit grouped under one Dose label. The active list uses captured E-Mareez row order/display values and mirrors the current retrievable reference rows: 200 table rows, Products Without Stock 60, Out of Stock 22, Low in Stock 12. Focused Chrome/CDP capture `data/emareez-import/products-medical-live-focused.json` verified E-Mareez currently reports page ranges through `201-209 of 209`, but page 5 contains only `No Data Found`; local API returns 200 rows, matching the actual retrievable reference table data. User-created local products now stay visible in the same reference-backed product pages instead of disappearing behind `reference_only=true`; temporary local API verification created a medical product, found it by search, included it in Products Without Stock, reported it, restored it, and cleaned it up. Fresh Chrome/CDP verification after a clean login confirmed the Medical Products page renders the reference rows, alert counts, and `1-50 of 200` footer. The Add Medical Product dialog now uses a scoped wide product modal matching E-Mareez's full-width large dialog treatment; Chrome/CDP verified 1200px width, three equal columns, captured labels/placeholders, and no native selects inside the modal. Row actions are icon-only. Fresh Chrome/CDP verification created a temporary reported medical product, confirmed the Reported Medicines tab rendered a blank visual action cell with SVG Edit/Restore buttons, then hard-deleted the temporary product. |
| Non Medical Products | Product CRUD for non-medical items | Partial | Add/edit/report/restore UI wired; active/reported tabs, server-backed reference search/pagination, E-Mareez-style warning messages plus checkbox stock filters, and Add Non Medical Product modal fields match the captured reference labels/placeholders with Weight and Unit grouped under one Weight label. The active list uses the 17 captured E-Mareez rows/order/display values and mirrors visible counts: Products Without Stock 5, Out of Stock 1, Low in Stock 0. The shared product listing now includes user-created local products alongside captured E-Mareez reference rows while keeping stale imported local rows hidden; temporary local API verification created a non-medical product, found it by search, included it in Products Without Stock, moved it to reported, and cleaned it up. Chrome/CDP verified the shared checkbox alert structure and `1-17 of 17` footer. The Add Non Medical Product dialog now uses the same scoped wide product modal; Chrome/CDP verified 1200px width, three equal columns, captured labels/placeholders, and no native selects inside the modal. Row actions are icon-only. Fresh Chrome/CDP verification created a temporary reported non-medical product, confirmed the Reported Non-Medicine tab rendered a blank visual action cell with SVG Edit/Restore buttons, then hard-deleted the temporary product. |
| Medicine Formula | Formula setup/list | Partial | Add New Formula Name and server-backed search/pagination are implemented. The visible table now matches E-Mareez for headers and first-page rows with no Actions column and blank descriptions rendered empty. A robust 72-page Chrome/CDP capture imported all 3,572 captured formula rows, preserves duplicates/order via `reference_sort_order`, and widened formula descriptions to `TEXT` so long E-Mareez descriptions are not truncated. Fresh Chrome/CDP capture verified the Add New Formula Name modal title and Name/Description field set while the first-page table still has zero row/header mismatches and `1-50 of 3572`. API multiset comparison shows `missing: 0`, `extra: 0`; referenced formulas return a controlled 409 instead of database failure on delete. |
| Category | Category CRUD | Partial | Add New Category and server-backed search/pagination are implemented. The visible table now matches E-Mareez with Category Name, Type, and a blank Actions column; imported 331 captured rows with duplicate entries preserved, reference ordering, numbered pagination, and Medical/Non Medical display labels. Fresh Chrome/CDP verified first-page headers/rows and `1-50 of 331` footer with zero mismatches while the Add New Category modal is open. The modal Type control now matches E-Mareez as a text/autocomplete input showing `Medical` instead of a native select leaking `Select an option / Medical / Non Medical`; Category Name input and Add action match the captured reference. Referenced categories return a controlled 409 instead of database failure on delete. |
| Manufacturer | Manufacturer CRUD | Partial | Add New Manufacturer and server-backed search/pagination are implemented. The visible table now matches E-Mareez with only the Manufacturer Name column, no local-only Actions column, imported 2,465 captured rows with duplicate entries preserved, reference ordering, and numbered pagination. Fresh Chrome/CDP verified the Add New Manufacturer modal title/input shape and first-page headers/rows with `1-50 of 2465` footer and zero mismatches. Referenced manufacturers return a controlled 409 instead of database failure on delete. |
| Supplier | Supplier CRUD | Partial | Add/edit/server-backed search/delete UI wired; refreshed the live E-Mareez supplier capture to 52 rows in `data/emareez-import/suppliers-live.json`, importer now preserves all captured pages, and row actions are icon-only so the table text matches E-Mareez while local edit/delete remain functional. Chrome/CDP verified both captured Supplier pages with matching headers, 52 rows, zero mismatches, and `51-52 of 52` footer. Backend hides soft-deleted suppliers by default and keeps inactive rows recoverable with an explicit API flag. Fresh Chrome/CDP captures verified the Supplier modal field set and placeholders match E-Mareez: Supplier Name/Company, Contact Person, Phone#, Email, and Add in the wider three-column layout. Temporary local lifecycle verification created a supplier, edited name/contact/phone/address, attached a temporary batch, verified computed Total Batches/Stock Purchase Price/Paid Amount/Supplier Outstanding/payment status, verified supplier batch/outstanding endpoints, soft-deleted the supplier, confirmed active search hides it and `include_inactive=true` recovers it, then hard-cleaned the temporary supplier/product/batch rows. Remaining risk is mostly visual parity for uncommon supplier action states. |
| Shelf | Shelf CRUD | Partial | Add New Shelf and server-backed Search Medicine/pagination are implemented. The visible table now matches E-Mareez with Name, Total Batches, and an Actions column; captured reference total-batch counts are stored on shelves so `Shelf 1` displays `11` like E-Mareez. Row edit/delete actions are now wired with the same compact icon controls used by other reference-style tables. Fresh Chrome/CDP verified the Add Shelf modal title/Shelf A input and first-page headers/rows with `1-8 of 8` footer and zero mismatches. Temporary local lifecycle verification created, searched, edited, and deleted an empty shelf, then created a shelf attached to a temporary product/batch and confirmed delete returns the controlled 409 in-use response; all temporary shelf/product/batch rows were hard-cleaned. |
| Stock Purchase | Stock purchase history | Partial | Server-backed search, purchase-date filter, print stock history, reference column order, dd/mm/yyyy date display, and real paged totals implemented. The page now uses the 213-row captured E-Mareez stock purchase table from `data/emareez-import/stock-purchases-live.json`; Chrome verified first-page rows, headers, blank invoice cells, and `1-50 of 213` footer. Print Stock History now fetches the complete filtered result set instead of only the visible page, preserves blank stock-history cells, renders a landscape Hassan Pharmacy stock report, and includes the software footer. Chrome/CDP intercepted the print output and verified it contains `Total Records: 213`, the first captured row (`AVSAR 80/5MG TAB`), a late captured row (`Panadol 500mg`), and `Software By Hassan Pharmacy`. Final pixel-level print CSS parity remains pending. |

## Staff And Settings

| Feature | Target Behavior | Status | Notes |
| --- | --- | --- | --- |
| Staff Management | Staff list, create/edit, roles | Partial | Add/edit/block UI implemented with server-backed search/pagination and real totals; imported the 16 captured E-Mareez staff rows into the local Docker DB, hides owner/admin system users from staff lists by default, and orders rows to match the captured reference. Chrome/CDP verified the current Staff table headers and 16 visible rows with zero mismatches, removed the local-only footer when all rows fit, added the reference-style name avatar, and converted row actions to icon-only View, Edit, Block, Edit Pin, and Edit Permissions cells. Add Staff now matches the captured E-Mareez three-step flow: step 1 fields/roles/gender options, step 2 `Create 6 Digit Pin for your staff member` with six one-character PIN boxes and Next only, and step 3 `Select All Permissions` with the 17-row View/Edit/Add/Delete matrix, 40 initially unchecked boxes, and Back/Done actions. Chrome/CDP captured `emareez-staff-add-modal-step3-real-full.json` and verified local `local-staff-modal-step3-after-permission-patch.json`. Staff permissions are now enforced beyond storage: frontend filters sidebar routes, hides topbar Add New Sale when denied, redirects direct forbidden routes to the first allowed page, and backend middleware maps API path/method to the same permission keys before allowing non-owner/admin users. Live local API verification created temporary staff with only Dashboard + Sales History permissions; `/api/reports/sales-summary` returned 200, `/api/suppliers` returned 403 `Permission denied`, and cleanup deactivated the temporary staff. Chrome/CDP route probe `reference-captures/local-staff-permission-route-probe.json` logged in a restricted staff session, attempted `/pms/dashboard/supplier`, redirected to `/pms/dashboard`, removed Supplier/Add New Sale from visible text, then restored the browser to admin. Earlier temporary destructive workflow verification created a cashier with password, sales PIN, and permissions; confirmed duplicate email rejection; edited name/phone/address/role/gender/sales PIN/permissions; verified active search; soft-blocked the staff row; verified it disappeared from active listing and appeared with `is_active=false`; then hard-cleaned the temporary user. Remaining risk is visual parity for less-used populated staff action modals after the current data changes. |
| Shift Management | Shift list and assignment | Partial | Add/edit/delete UI implemented with server-backed search/pagination and real totals, count/card layout, structured off-days, staff display names, and AM/PM start/end controls; Assign Shift modal no longer exposes the extra Date field shown only by our backend model. Chrome/CDP verified the local list now matches the current E-Mareez empty state with count `0`, no pagination/footer, no extra empty-state heading, and `No Data Found`. Fresh Chrome/CDP captures verified the Add Shift modal field set matches E-Mareez: Staff Member, Shift Type, Off Days, Start Time, AM, End Time, AM, and Add. Backend update validation now accepts date edits instead of rejecting them as `None`; temporary local API verification created a shift, searched it by staff, edited shift type/time/off-days, deleted it, and confirmed totals returned to zero. A separate partial-update probe verified date-only edits preserve the existing shift type. Remaining risk is visual parity for populated shift cards because E-Mareez currently has no captured populated rows. |
| Expenses | Daily expenses and categories | Partial | Expense and category add/edit/delete UIs implemented; date/date-range expense filters, list-level Download Expense PDF, per-row PDF download, and expense/expense-category search/pagination now call the server with real totals and responses include category display names. List-level expense PDF now exports the complete filtered result set instead of silently limiting to 500 rows and rendering only the first 24: direct generator verification produced an 80-row / 3-page PDF containing first row, last row, and `Total Expenses: 80`; live API smoke test confirmed `/api/expenses/download-pdf` returns `%PDF-1.4` with Hassan Pharmacy, Daily Expense Report, and Total Expenses text. Expense Category imports all 309 captured E-Mareez rows, including literal `- / -` rows, preserves reference ordering via `reference_sort_order`, renders empty descriptions blank, and uses icon-only edit actions. Fresh Chrome/CDP table scrape verified Expense Category first-page headers/rows with zero mismatches and `1-50 of 309` footer while the Add Category modal is open; the modal matches Add Category title, Office Supplies category-name input, Description textarea, and Add action. Fresh Chrome/CDP verified Expense empty-state parity still hides empty pagination and uses `No Data found!`; Add Expense now matches the captured input types/placeholders more closely with Expense Name text, slash-style Date `tel` field, text/autocomplete-style category control, Amount text field, Description textarea, and Add action. Expense edit validation now accepts date edits and partial payloads; temporary local API verification created an expense, edited date/name/amount, searched the edited row, deleted it, and confirmed cleanup. Expense Category delete now returns a controlled in-use 409 and was verified with temporary local records. |
| Return Policy Notes | Manage return policy notes | Done | Policy and instruction add/edit/delete UIs implemented. Chrome/CDP verified captured table headers, policy text, instruction row order, Add Policy modal with Description textarea/Save, and Add Instructions modal with Title/Description textareas/Save. Backend return-note ordering now matches captured E-Mareez order. Fresh Chrome/CDP verification confirmed both policy and instruction rows now use blank visual action cells with SVG Edit/Delete icon buttons. |
| Change Password | Change current account password | Done | Staff/Admin toggle, three-password-field layout, and per-field password visibility toggles match the captured E-Mareez structure. Chrome/CDP verified Staff state with both captured headings, Admin state with account-password heading, hidden-by-default fields, Show/Hide toggle behavior, and Save action without submitting the form. |
| Get Technical Help | Static support contact page | Done | Direct centered page layout, heading, phone/email links, dark left-arrow GO BACK TO DASHBOARD action, Hassan Pharmacy branding/contact replacement, and back navigation to dashboard verified in Chrome/CDP. |

## Reference Assets

- Latest full sidebar/page audit: `reference-captures/e-mareez-sidebar-audit/`.
- `page-summaries.json` contains extracted URLs, buttons, inputs, table headers, and modal summaries.
- `sidebar-groups.json` contains the expanded sidebar tree for Sales, Inventory, Daily Expense, and Settings.
- Reference captures also live in `reference-captures/` and `live-capture/`.
- Generated live table data used for imports lives in `data/emareez-import/`.
- Use those images/json files before changing a page for parity.
- Add new reference screenshots when an E-Mareez behavior is discovered.

## E-Mareez Reference Audit - 2026-07-01

Captured from the logged-in E-Mareez Chrome tab. Screenshots are named by sidebar order in `reference-captures/e-mareez-sidebar-audit/`.

### Sidebar Tree

- Dashboard
- Sales
  - New Sale
  - Return Item
  - Sales History
  - Return History
  - Product Sales History
  - Customer History
- Inventory
  - Batch
  - Demand/Order
  - Stock Audit
  - Order Purchase
  - Medical Products
  - Non Medical Products
  - Supplier
  - Category
  - Medicine Formula
  - Manufacturer
  - Stock Purchase
  - Shelf
- Staff Management
- Shift Management
- Daily Expense
  - Expense
  - Expense Category
- Settings
  - Change Password
  - Return Policy/Notes
- Get Technical Help

### Page And Control Map

| Sidebar page | Reference route | Primary controls | Table columns / visible behavior | Modal captured |
| --- | --- | --- | --- | --- |
| Dashboard | `/pms/dashboard` | Topbar Add New Sale, KPI cards, report panels | Dashboard metrics and expired batch/report panels | No |
| New Sale | `/pms/dashboard/newsale` | Tabs: New Sales, Recent Sales, Draft Sales; Add to List | Entry table: Product Name, Bar Code, Sale Type, Quantity, Action. Purchased list: Product Name, Batch no., Qt in Box, Qt in Units, Total Quantity, Cost Price, Rate/Sell Price, Amount, Discount(%), Discount(Amt), Payable Amt, Actions | Validation dialog when Add to List is clicked without fields |
| Return Item | `/pms/dashboard/return-item` | Original invoice input, Generate Invoice | Batch no., Name, Quantity Sold, Quantity Returned, Rate/Sell Price, Amount, Action | No |
| Sales History | `/pms/dashboard/saleshistory` | Date / Date Range radios, date/time filters, search by invoice/medicine, Print Sales History, rows-per-page, pagination | History table loads after filters/search; eye opens Sales History Details modal | Print option modal: Current Page Only, Entire Sales History, Current Page Summary, Entire Sales History Summary |
| Return History | `/pms/dashboard/returnhistory` | Select Date / Select Date Range radios, date filter, invoice search, All selector, rows-per-page, pagination | Invoice Number, Date, Time, Total Amount(RS.), Discount(%), Discount Amount, Total Payable, Paid, Due(s), Change Returned, Action | No |
| Product Sales History | `/pms/dashboard/productsaleshistory` | Select Date / Select Date Range radios, date filter, search, rows-per-page, pagination | Product Name, Dose, Generic Name, Sold Quantity, Action | View Product History modal with invoice rows and View / Print Invoice actions |
| Customer History | `/pms/dashboard/customer-history` | Search, rows-per-page, pagination | Name, Phone#, Due Amount, Actions | Edit Customer; Payment History with invoice rows, totals, payment methods, receive-now, Clear Dues, View and Print Invoice actions |
| Batch | `/pms/dashboard/batch` | Active/Reported tabs, Date/Date Range filters, In Stock filter, search, rows-per-page, Add New Batch | Batch No., Shelf No., Name, Medicine Formula, Stock In, Stock Out, Stock Remaining, Stock Purchase Price, Stock Purchase Price Before Tax, Sell Price, Cost Price, Total Cost, Production Date, Expire Date, Created at, Supplier Name, Paid Amount, Supplier Outstanding, Supplier Invoice no., Actions | Add Batch |
| Demand/Order | `/pms/dashboard/demand` | Supplier, medicine, type/quantity inputs, Add To List, search | Supplier Name, Product Name, Type, Quantity, Action/Actions | Validation dialog when supplier missing |
| Stock Audit | `/pms/dashboard/stock-audit` | Medicine search, quantity, adjustment selector, Add To List, SAVE | Product Name, Batch, Type, Quantity, Select Adjustment; audit list includes before/after quantities, adjustment type, amount, actions | Validation dialog when medicine missing |
| Order Purchase | `/pms/dashboard/order-purchase` | Medical / Non Medical radios, search, rows-per-page, pagination | Medicine Name, Dose, Total Quantity, R. Quantity, Manufacturer Name, Actions | No |
| Medical Products | `/pms/dashboard/medicines` | Active/Reported tabs, search, stock alerts, rows-per-page, Add New Medicine | Name, Formula, Dose, Total Quantity, Remaining Quantity, Generic Name, Category, Manufacturer Name, Actions | Add Medical Product |
| Non Medical Products | `/pms/dashboard/nonmedicines` | Active/Reported tabs, search, stock alerts, rows-per-page, Add New | Name, Brand Name, Category, Total Quantity, Remaining Quantity, Actions | Add Non Medical Product |
| Supplier | `/pms/dashboard/supplier` | Search, rows-per-page, Add New Supplier | Supplier Name/Company, Contact Person, Phone#, Email, Total Batches, Stock Purchase Price, Paid Amount, Supplier Outstanding, Status, Actions | Add New Supplier |
| Category | `/pms/dashboard/category` | Search, rows-per-page, Add New Category | Category Name, Type, Actions | Add New Category |
| Medicine Formula | `/pms/dashboard/medicineformula` | Search, rows-per-page, Add New Formula Name | Medicine Formula Name, Description | Add New Formula Name |
| Manufacturer | `/pms/dashboard/manufacturer` | Search, rows-per-page, Add New Manufacturer | Manufacturer Name | Add New Manufacturer |
| Stock Purchase | `/pms/dashboard/purchases` | Search, rows-per-page, pagination | Batch No., Medicine Name, Quantity, Rate, Total Amt, Extra Discount/Bonus, Purchase Date, Stock Cost Price, Sales Tax, Invoice ID, Expire Date, Supplier Name | No |
| Shelf | `/pms/dashboard/shelf` | Search Medicine, rows-per-page, Add New Shelf | Name, Total Batches, Actions | Add Shelf modal uses one Shelf Name field with Shelf A placeholder and Add action; batch quick-add shelf flow uses the same labels |
| Staff Management | `/pms/dashboard/staff-management` | Search Name, Add Staff | Name, Role, Phone #, Address, Email, Actions | Add Staff modal now matches the captured E-Mareez three-step flow: step 1 roles include Pharmacist/Technician/Assistant/Manager/Intern/Stock_manager/Cashier and Gender only Male/Female; step 2 has six PIN inputs with Next only; step 3 has Select All Permissions plus the 17-row View/Edit/Add/Delete permission matrix with 40 initially unchecked boxes and Back/Done actions. Chrome/CDP captured `emareez-staff-add-modal-step3-real-full.json` and verified local `local-staff-modal-step3-after-permission-patch.json`. |
| Shift Management | `/pms/dashboard/shift-management` | Search Name, Add Shift | Shift cards/list with count | Assign Shift |
| Expense | `/pms/dashboard/daily-expense` | Date / Date Range radios, date filter, Download Expense PDF, Add Expense | Date, Name, Expense Category, Expense Amount, Actions | Add Expense modal matches captured labels/placeholders: Expense Name, Date, Expense Category, Amount, Description, Add |
| Expense Category | `/pms/dashboard/expense-category` | Search Category, rows-per-page, Add New Category | Category Name, Description, Actions | Add Category modal matches captured title/placeholders: Office Supplies and Enter Description... |
| Change Password | `/pms/dashboard/settings/change-password` | Staff/Admin toggle, current/new/verify password with visibility toggles, Save | Form only | No |
| Return Policy/Notes | `/pms/dashboard/return-policy` | Add New Policy, Add New Instructions | Policy Description, Actions; Title, Description, Actions | Add Policy modal uses Description textarea and Save; Add Instructions modal uses Title/Description textareas and Save |
| Get Technical Help | `/pms/dashboard/technicalhelp` | Static contact/help page | Heading: For any Query Contact Hassan Pharmacy Care; phone, email, and dark left-arrow GO BACK TO DASHBOARD action | No |

### Captured Modal Field Sets

| Modal | Fields / behavior captured |
| --- | --- |
| Add Batch | Product Type medical/non-medical; Bar Code; Supplier; Name; Batch No.; Total Boxes; Units per box; Total Stock; Cost Price unit/box; Sell Price unit/box; Stock Purchase Price Before Dis.; Extra Discount/Bonus percentage/amount; Stock Purchase Price; Sales Tax percentage/amount; Total Stock Price Before Tax; Expire Date; Shelf; Production Date; Paid Amount; Supplier Outstanding; Purchasing Method; Show More fields captured separately in prior screenshots. |
| Add Medical Product | Bar Code, Name, Medical Formula, Category, Dose, Unit, Generic Name, Manufacturer Name, prescription checkbox. |
| Add Non Medical Product | Bar Code, Name, Brand/Manufacturer Name, Weight, Unit, Category. |
| Add New Supplier | Supplier Name/Company, Contact Person, Phone#, Email. |
| Add New Category | Type, Category Name. |
| Add New Formula Name | Name, Description. |
| Add New Manufacturer | Manufacturer Name. |
| Add Staff | Multi-step: Add Staff, Assign Pin, User Permissions. First step fields: Name, Role, Email, Number, Address, Gender, Sales PIN. Second step: Create 6 Digit Pin for your staff member with six one-character inputs. Third step: User Permissions. |
| Assign Shift | Staff Member, Shift Type, Off Days, Start Time with AM/PM, End Time with AM/PM. |
